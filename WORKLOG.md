# WORKLOG —— 工作日誌

> 這個專案由多個 AI agent 輪流接手（ChatGPT Codex、Claude Code），各自在不同的
> 作業階段與帳號下執行。**接手前先讀這份，再讀 `README.md` 與 `app/about.md`。**
>
> **每次改動都要在「變更紀錄」補一筆。** 只寫從程式碼和 `git log` 看不出來的東西：
> 為什麼這樣選、試過什麼結果不行、量到的數據、還沒決定的事、踩過的環境雷。
> 程式碼本身說得清楚的（改了哪個函式、加了哪個參數）不要抄進來。
>
> **每一則的標題都要有日期**，格式是 `### 2026-09-14（第四十八次）—— 一句話講結論`。
> **而且新的一則加在檔案最後面** —— 這份日誌由舊到新往下長，最下面那則就是最新的。
> 2026-09-14 以前的排法是反過來的（新的在最上面），那天一次性全部倒轉。
> 所以早期紀錄裡的「上一則」「前一則」指的都是**時間上更早**的那則，
> 倒轉之後它剛好就在正上方，讀起來才是對的。
>
> **每一則都要標上 session id**，格式是標題底下第一行寫 `` `session: <id>` ``。
> 這個專案**同時**有多個 session 在改——2026-09-12 一天之內就有三個以上交錯提交，
> 沒有署名就無從判斷一則紀錄是誰寫的、哪一則比較新、該去問誰。
> 已經出過事：某個 session 寫下「104 passed / 0 failed」，隔天實際跑是紅的
> （見第三十三次），當時無法得知那句話出自哪個 session、在什麼條件下量的。
>
> id 從哪裡來：Claude Code 用 `ListAgents`，輸出第一行就是
> 「This session is …」；其他 harness 用它給的識別碼；真的沒有就寫工具名稱與日期。
> **2026-09-13 之前的紀錄沒有署名，而且補不回來**——session id 沒有記錄在任何地方，
> 硬猜只會製造假的可信度。下面只有確定出自 `e01f5e` 的幾則補上了。
>
> 這份檔案**進版控**，因為它的用途就是跟著 repo 交接給下一個接手的人或 agent。
> 真正的金鑰放 `.streamlit/secrets.toml`（已 gitignore），任何情況都不要寫進這裡。

---

## 目前狀態

**最後更新：2026-09-15**

- 分支：`main`，**領先 `origin/main` 四個 commit**（第五十三次的文件與第五十四次的提速，等待推送）
- **首頁冷啟動由 15–23 秒降到 2–6 秒**（2026-09-15，第五十四次）：證交所名錄改為
  先給內建快照、網路版在背景追上，解析改用 regex。**瓶頸從來不是股價**，是那份 8 MB 的 HTML
- **2026-09-15 修掉一個會讓所有標的都取不到行情的缺陷**（第五十三次）：
  Yahoo 給了一列「有成交量、沒有收盤價」的資料，`unit_breaks()` 在 `round(nan)` 丟例外。
  本機已修並重啟，`c9dc462` 已推上 `origin/main`（見「待驗證」）
- **新增「緩衝池退休法」分頁**（2026-09-14，第五十次）：解釋退休5／退休6 的提領規則、
  每個數字的用意與代價，並顯示 `analysis/retirement5-original-100-12.png`。
  **那張圖因此成為 APP 的執行期資產，不可刪**
- **預設配置現在有 7 個**：衝刺、退休1–4（四檔持有不再平衡）、
  **退休5**（009826 貝萊德世界股票 90% ＋ 00865B 國泰US短期公債 10%）、
  **退休6**（VT 90% ＋ 00865B 10%，VT 以美元計價、自動換算台幣）。
  退休5／6 是緩衝池法，退休6 是退休5 的長歷史替身（見第四十、四十一、四十二、四十三次）
- **APP 現在吃得下美元標的**（VT、SPY…）：`load_symbol` 內部換算台幣，
  畫面會揭露；`^TWII` 與 `.TW/.TWO` 視為台幣不換算（見第四十三次）。
  **名稱也查得到了**（2026-09-14，第四十七次）：美股向 Yahoo 查英文名，
  台股一律不查以免中文名被蓋掉；`CATALOG` 手寫的名字優先（VT 仍是中文）
- **上游資料有兩支是壞的，APP 會自動校正並說明**：0052 於 2025-11-17 分割 1:7、
  0050 於 2014-01-02 分割 1:4，Yahoo 兩次都沒記錄（見第三十六～三十九次）。
  2317、3008 是除權配股（非整數比例），**只警告不校正**
- **2026-11-17 00662 分割 1 拆 5**，已排進「待驗證」，程式不需要改動
- 舊狀態（2026-09-12）：`9d29a44` 是修雲端熱重載的；
  24 個 commit 於當日分九批推送（`c44d0ec..82c8d72`）。
  **沒有動任何設定格式**，雲端 Secrets 至今不需要跟著改
- **雲端與本機用的是兩把不同的 token，但額度仍然共用** ——
  HF 的免費額度是**綁帳號不是綁 token**（官方定價頁：Every Hugging Face *user*
  receives monthly credits，Free Users $0.10）。
  雲端另產一把的價值是可以單獨撤銷，不是多一份額度。
  所以額度耗盡期間雲端的 AI 按鈕一樣點不動，
  新 prompt 是否真的生效要等額度恢復才驗得到（見「待驗證」）。
  例外：若雲端那把來自**另一個 HF 帳號**，它有自己的 $0.10 —— 按一次按鈕就知道
- 雲端 Secrets（`HF_TOKEN` / `HF_MODEL`）已由使用者於 App settings 設定；
  `HF_MODEL` 維持 `zai-org/GLM-4.7:fastest`，這批改動沒有動到任何設定格式
- 開發分支 `feature/ai-allocation-advisor` 已 fast-forward 併入，可刪
- **`abandoned/model-fallback` 只存在於原開發機**，從未推送。
  在新 clone 上找不到它——要保留那份程式碼得先 `git push origin abandoned/model-fallback`
- 測試：**160 passed / 0 failed**（全綠）；另有 `app/tests/smoke_live.py`
  實機整合檢查 **39 項全過**（會打網路，不在 CI 內）
- **HF 免費額度已耗盡（2026-09-12）**，AI 分頁會顯示額度用完並附帳單連結。
  每月初重置，即 2026-10-01；要立即恢復可買 credits（較划算）或升級 PRO（$9／月）。
  在那之前**任何需要實際呼叫模型的驗證都做不了**（見「待驗證」）
- 本機 App：`http://127.0.0.1:8501`，AI 分頁串流輸出。
  **`run.bat` / `start.ps1` 現在會強制重啟**（2026-09-14，第四十六次）：
  先砍掉舊行程再開新的，所以改完程式雙擊就會看到新版。
  用別的方式啟動仍會撞到舊的坑——見環境陷阱 5b。
  換成六段 prompt 後輸出約變兩倍（718 → 1300–1440 tokens），
  緩衝式呼叫實測 7–9 秒（原本 4.3 秒）；串流的首字時間不受影響

## 進行中

把 AI 解讀從「附屬在相關性分頁的小面板」做成完整功能。原始需求是
**「分析並評論 APP 上設定的投資配置，並提出專業的理財規劃建議」**。

分四步，目前完成第 1、2 步：

1. ✅ AI 解讀獨立成分頁，放在「投資報酬」之後
2. ✅ 放寬 system prompt 分寸，改為輸出評論式內容（當時是三段，現行 prompt 已是六段，見第二十二次）
3. ⬜ 補上理財規劃需要的輸入（投資年期、每月可投入、目標、風險承受度）
4. ⬜ 視情況做 BYO key（讓使用者用自己的 HF token）

第 3 步自 2026-09-11 起就沒再動過。之後（第十七～二十二次）做的都是既有功能的
可讀性與正確性修整，不在這四步之內：完整輸入可複製、退休配置擴成四個、
共同期間提示收斂、金額加萬元第二行、prompt 改六段框架。
**下一個實質功能仍然是第 3 步。**

## 待驗證

### 2026-11-17：00662 分割 1 拆 5

已公告（富邦投信）：**11/11–11/16 停止交易、11/17 恢復並以新單位掛牌**。
00662 在四個退休配置裡都佔 20–30%，所以這次會直接影響使用者自己的配置。

演練過了（見第三十九次），程式不需要任何改動。**當天要確認的是**：

1. 行情比較頁有沒有出現「已自動校正…00662：2026/11/17 分割 1:5」那行說明
2. 區間報酬有沒有變成負的（沒校正到的徵兆）
3. 若 Yahoo 這次**正確記錄**了分割，則完全不會有步階、也不會有說明——那也是對的

若沒抓到，先看是不是漂移超出 10%（停牌四天期間那斯達克 100 的累積漲跌），
那種情況會退化成「只警告不校正」，不會產生錯誤數字。

### 2026-09-15 推送之後：正式站的行情有沒有回來

`c9dc462` 已推上 `origin/main`（第五十三次），會觸發自動重新部署。
推送前的正式站應該是壞的——同一份程式、同一個 Yahoo，沒有理由倖免。

**這批不是第三十次那種形態**：只動了 `unit_breaks()` 的函式內部，
沒有新增或移除任何模組層級的名字，所以不需要靠 `_refresh_replaced_modules()` 救。

開站後看一件事就夠：**側邊欄任選一個預設配置，行情有沒有出來。**
若仍顯示「目前無法取得行情。請檢查網路或稍後重新取得資料。」，
**不要去查網路**——展開「資料問題與處理方式」看 failure 的原文才知道是什麼。

**網址是 https://taiwan-market-lens.streamlit.app/** （2026-09-15 由使用者提供，已寫進 README）。
**但它是登入制的**：未登入時連 `/_stcore/health` 都會 303 轉到 `share.streamlit.io/-/auth/app`，
所以**不存在可匿名探測的健康檢查端點**，任何外部監控只看得到那個轉址，
無論 app 是好的、壞的、還是睡著的。要自動監控就得帶登入憑證。

### 2026-09-14 推送之後：雲端有沒有活著

`836e1b9` 已推上 `origin/main`（第四十六、四十七次），會觸發自動重新部署。
**這批改動動到了 import 結構**——`streamlit_app.py` 多了
`from market import ... currency_of, dollar_name`，正是第三十次讓雲端在部署當下整個掛掉的形態。
已確認 `_refresh_replaced_modules()`（第 61 行）跑在那個 import（第 66 行）之前，所以理論上守得住，
但**沒有實際驗過**：正式站網址沒有記錄在這個 repo 的任何檔案裡，本機無從確認。

開站後要看的兩件事：

1. App 有沒有起來（掛掉的話 traceback 會直接寫在頁面上，會是 ImportError）
2. 側邊欄加一檔 `VOO`，看名稱是不是 `VOO Vanguard S&P 500 ETF` 而不是「名稱暫未取得」。
   雲端與本機打的是同一個 Yahoo 端點，但雲端的對外連線是另一回事，值得看一眼

**順手把網址寫進這裡**，下一個接手的人就不用再問。

額度恢復（2026-10-01 或提前購買）之後**第一件要做的事**：

**確認第二十二次補的繁中規則有效。** 產業用語的簡體字（「产业链」「供应链」「走势」）
是實測抓到的，補了三個對照例子之後**還沒有機會驗證**。
不必重跑腳本 —— 在 App 的 AI 分頁點一次「AI 深入解讀」，看輸出有沒有簡體字即可。
若仍漏，下一步不是再加例子（會變成打地鼠），而是把繁中要求移到 prompt 最後
（結尾的指示通常比開頭更容易被遵守），或在送出前做一次簡繁檢查。

不花額度的替代方案：用 AI 分頁的「取得完整輸入（含 Beta 與配息）」複製鈕
（第十七次做的），貼到任何聊天模型跑一次。那份內容取自 `active_prompt()`，
拿到的就是現行這版指示。

## 待決定

**退休評分功能還沒開工**，但方向在第四十次改了：**要比較的是提領規則，不是配置**。
配置在四種方法下都分不出來（第三十四、三十五、四十次），而換提領規則可以把破產機率
從 19% 變成 0%。待做的規則至少有四種：固定實質金額、固定比率、緩衝池（三年）、護欄法。
還缺輸入（退休年齡／規劃到幾歲／年開銷／預期通膨／質押利率／其他收入）與稅的處理。
**動手前先解決匯率瓶頸**：`TWD=X` 只到 2004-03，而 MSCI World 指數有 54.7 年，
改用美元基準可換到四次完整空頭（見第四十次）。

**第 3 步還沒做**：payload 缺理財規劃需要的脈絡（投資年期、每月可投入、
目標金額、風險承受度），app 目前沒有這些輸入。沒有它們，AI 只能講配置通則，
給不出真正個人化的規劃討論。這是「理財規劃建議」與「配置評論」的分界線。

---

## 環境陷阱

踩過的，會再踩的：

**1. `.git` 的擁有者不是 `ssp62`。**
Codex 在沙箱帳號 `MSI\CodexSandboxOffline` 下跑，`.git` 歸它所有。
以 `ssp62` 執行 git 會被 dubious ownership 擋下。已加例外：

```bash
git config --global --add safe.directory 'C:/Data/project/Codex/Codex APP 0908'
```

程式檔案本身（`app/**`）owner 是 `ssp62`，ACL 有 `Authenticated Users: Modify`，
兩個帳號都寫得進去。**但不要兩個 agent 同時改同一個檔案。**

**2. Streamlit 的 markdown 會把一對 `$` 當成 LaTeX。**
訊息裡出現第二個錢字號時，兩者之間整段會變成數學字體、錢字號本身消失（見第二十四次）。
要顯示金額走 `ui.plain()`。一個 `$` 看起來沒事，所以這個坑只在加第二個時才爆。

**3. 主控台是 cp950，中文會變亂碼、emoji 會直接炸。**
所有會輸出中文的指令前面加 `PYTHONIOENCODING=utf-8`。

**4. `.pytest_cache` 寫入被拒（屬於沙箱帳號）。**
跑測試加 `-p no:cacheprovider`，否則會噴一堆 PytestCacheWarning：

```bash
PYTHONIOENCODING=utf-8 ./.venv/Scripts/python.exe -m pytest -q -p no:cacheprovider
```

**5. Streamlit 不會重新 import 子模組（雲端部署也一樣）。**
hot-reload 只重跑主腳本，`import` 進來的模組留在 `sys.modules` 快取。
**新增函式、新增模組、改 import 結構之後一定要重啟服務**，否則會出現
「Streamlit 顯示的原始碼是新的、實際執行的是舊的」這種極度誤導的 traceback。
（`app/module_compat.py` 的 `load_renderer` 就是為了對付這件事，但它救不了自己。）
**雲端自動重新部署走的是同一條路**：只重跑入口腳本、不重載子模組，
所以新版一旦 import 舊模組沒有的名字，App 會在部署當下整個掛掉（見第三十次）。
`app/streamlit_app.py` 開頭的 `_refresh_replaced_modules()` 就是為此而存在：
比對 `.pyc` 標頭裡的原始碼戳記與磁碟上的檔案，對不上就 reload。
**不需要維護任何名單**（第三十次用哨兵名單的版本擋不到「函式多一個參數」，見第三十一次）。

**5b.（2026-09-14 起只剩一半）本機「重啟」曾經多半不是重啟。**
症狀是「改了程式、重啟了、畫面還是舊的」，由兩件事疊出來。
**第一件已經修掉**：`run.bat` / `start.ps1` 現在帶 `--restart`，會先砍掉舊行程再開新的
（見第四十六次），雙擊就等於重啟。**第二件還在**，而且只要繞過 `--restart` 就會再中：

- `launch.py` **不加 `--restart` 時仍然是可重複執行**的：偵測到同一個 App 還在跑就直接重用
  並開瀏覽器（印 `REUSE`），不會重開。直接 `streamlit run` 也一樣不會殺舊的。
  `--restart` 關不掉時（例如行程屬於另一個帳號），手動砍：

```powershell
Get-CimInstance Win32_Process -Filter "Name='python.exe'" |
  Where-Object { $_.CommandLine -like '*Codex APP 0908*' -and $_.CommandLine -like '*streamlit*' } |
  ForEach-Object { Stop-Process -Id $_.ProcessId -Force }
```

  `app/launch.py --check` 會印 `START`（可啟動）或 `REUSE`（已在跑），用它確認。
  **`--check` 在 App 開機的那幾秒會誤報 `START`**：那時 port 已經綁住、但 app id 端點還沒起來，
  於是 `is_our_app` 為假、`port_free` 也為假，select_port 就跳到下一個埠。實測撞得到。

- **`_refresh_replaced_modules()` 比對的是「.pyc 對磁碟原始碼」，不是「記憶體裡的模組對磁碟」。**
  改完程式後只要在別的行程跑過 pytest，那次 import 就會把 `.pyc` 重寫成新的時間戳，
  於是伺服器行程看起來「沒有東西被換過」，但它記憶體裡拿的還是舊模組。
  實測：伺服器 06:36 啟動、`allocation.py` 06:41 修改，而 `.pyc` 內外都是 06:41 → 判定不需重載。
  **雲端不受影響**（那裡沒有第二個 Python 行程會去重寫 .pyc），所以這是本機開發限定的坑。
  症狀就是「新增的預設配置在下拉選單裡看不到」。

**6. HF 額度看不到餘額，只能從行為推。**
`whoami-v2` 不回傳任何額度或用量欄位，而 huggingface.co/settings/billing 的數字會落後
（伺服器已回 depleted 時，頁面仍可能顯示 $0.09／$0.10）。
**要判斷是不是真的耗盡，看 402 的 response body 原文**，HF 會直接寫明原因。
402 有兩種完全不同的情況，**要看趨勢不是看單次**：
暫時性的（等一下就回 200，見第十六次）與真的耗盡（頻率單調上升、怎麼等都不通）。
用 `max_tokens=1` 的輕量請求探測「恢復了沒」**不具代表性** ——
探測回 200、緊接著的完整請求照樣 402。要重試就重試真正那個請求，且上限 2–3 次就夠。
**免費額度是每月 $0.10，很薄。** 一天密集調校 prompt 就會用完，
而且用完之後整個 AI 功能就停擺到下個月初。要連續測就先買 credits 或升 PRO。

**7. 雲端部署不會帶上 `secrets.toml`。**
`.streamlit/secrets.toml` 在 `.gitignore` 裡（正確）。
部署後要去 Streamlit Cloud 的 App settings → Secrets 手動貼一次。
建議雲端另外產一把 token，方便單獨撤銷 —— **但那不會多一份額度**，
免費額度綁的是帳號不是 token，同帳號的兩把 token 共用同一個 $0.10。
另外 App settings **沒有 branch 欄位**，分支在建立 app 時決定、事後改不了，
所以更新正式站就是合回 `main` 再 push。

---

## Hugging Face 實測數據

用 `.streamlit/secrets.toml` 的 token 實際打出來的，**不是查文件推測的**。
要改模型或調參數之前先看這裡，可以省掉重測的錢和時間。

**計費**：免費帳號每月 $0.10 credits，用完要主動買才能繼續（不會自動扣款）。
**PRO 是訂閱 $9／月，附贈的額度才是 $2**（免費額度的 20 倍）—— 兩個數字不要搞混，
本檔先前寫成「PRO $2.00／月」是錯的，App 的 402 訊息也只寫了額度沒寫價格，已一併修正。
以本專案的用量（單次呼叫 `estimated_cost` 約 $0.00004–0.00015）$2 額度夠跑數百到數千次，
所以除非要像 2026-09-11 那樣連續密集調校，**直接買 credits 比訂 PRO 划算**。

**同一個模型在不同供應商的行為不一樣。**`:fastest` / `:cheapest` 後綴不只影響
速度和價格，還會換掉思考模式的預設值。以下是 1100 max_tokens、相同 payload 的實測：

| 模型與後綴 | enable_thinking | 耗時 | 正文 | 推理 | finish_reason |
|---|---|---|---|---|---|
| `GLM-4.7-Flash:fastest` | **False** | **3.1s** | 275 字 | 0 | stop ✅ |
| `GLM-4.7-Flash:fastest` | 預設 | 11.2s | **0 字** | 1667 字 | length ❌ |
| `GLM-4.7-Flash:cheapest` | False | 78.2s | 2664 字 | 0 | length ⚠️ |
| `GLM-4.7-Flash:cheapest` | 預設 | 29.7s | 394 字 | 0 | stop |
| `Qwen3.5-35B-A3B:fastest` | **False** | **3.1s** | 374 字 | 0 | stop ✅ |
| `Qwen3.5-35B-A3B:fastest` | 預設 | 8.5s | **0 字** | 3295 字 | length ❌ |

**結論**：`:fastest` + 明確傳 `chat_template_kwargs: {"enable_thinking": False}`。
不要依賴供應商預設，也不要用 `:cheapest`（慢，且輸出長度不穩定）。

繁中品質兩個模型都可用。GLM 在測試題上抓到「單一國家**與**單一產業」的雙重集中度，
Qwen 只講了國家那一層。DeepSeek 系列要留意簡體用詞（「投资组合」「盈利」）。

---

## 已評估但決定不做

### 多模型自動備援（2026-09-11 實作完成後，使用者決定放棄）
`session: claude-d1 [e01f5e]`

**程式碼已寫完並通過 81 個測試，保留在分支 `abandoned/model-fallback`（commit `524cd5e`），
未推送。日後想恢復：`git cherry-pick 524cd5e`。**

構想：`HF_MODEL` 接受逗號分隔清單，前一個模型異常就自動換下一個。
動機是當天遇到 `GLM-4.7-Flash:fastest` 把額度全用在推理、回傳空白正文。

**放棄的原因是部署順序，不是設計問題。** 金鑰與設定放在 Streamlit Cloud 的 Secrets、
程式碼放在 GitHub，兩者**無法原子性一起上線**。清單格式必須「先推程式、等部署完成、
再改 Secrets」；順序顛倒的話，舊程式會把整串逗號字串當成單一模型 ID 送出去，
得到 404 並顯示誤導的「請稍後重試」。使用者認為這個操作負擔不值得。

**若日後恢復，設計重點（已驗證）：**

| 狀況 | 是否換模型 | 理由 |
|---|---|---|
| 推理吃光、正文空白 | ✅ | 模型／供應商特性 |
| 429 限流、5xx | ✅ | 換模型等於換供應商 |
| 402 額度用完 | ❌ | 帳號層級，換幾個都一樣，只會加速燒完剩餘額度 |
| 401/403 金鑰 | ❌ | 同上 |

另外兩個關鍵：**已經吐出文字之後不可換**（否則畫面會出現兩篇黏在一起），
以及**備援不可隱形** —— 要標明「X 未能作答（原因），已改用下一個模型」，
讓使用者知道讀到的文字出自哪個模型。

### 由網址載入 prompt（2026-09-11 評估，使用者決定暫不實作）

構想：讓使用者貼上 prompt 網址（例如 GitHub 上的 prompt library），由 app 抓回來當
system prompt。**評估後決定不做。**

**先決問題：這類 repo 通常不是「一個 prompt」。** 以提案時舉的
`github.com/ckw19810413/traditional-ai-prompt-library` 為例，實際抓原始檔確認過：
四個 .md 檔、每檔 25 個 prompt（共 100 個），每個包在 ``` 裡、含 `{程式碼}` 這類待填變數，
外面還有標題與說明文字。整份餵給模型，它收到的是一份關於程式碼審查與寫 email 的文件。
**要用仍得自己挑一個出來、把變數換掉 —— 那就是複製貼上，現有的文字框已經能做。**

**若日後要做，三個坑：**

1. **GitHub 網址多半抓不到文字。** repo 頁面與 `blob/` 網址回傳的都是 HTML，
   要 `raw.githubusercontent.com` 才是純文字。使用者不會知道這個差別。
2. **SSRF。** 讓雲端上的 app 抓使用者指定的任意網址，等於把伺服器變成代理，
   可用來探測內網或雲端 metadata 端點。本 app 有登入限制所以風險可控，但這是實質漏洞類別。
3. **護欄會在看不見的情況下被替換。** 文字框的價值在於「套用前看得到內容」；
   網址載入則把內容藏起來，而那份內容會取代掉「不得自行計算」「不得給買賣指令」
   「不得誇大幅度」等規則。

**安全的形狀（約 40 行）**：白名單只允許 `raw.githubusercontent.com` 與
`gist.githubusercontent.com`；自動把 `github.com/.../blob/...` 轉成 raw；
抓回來**填進文字框而非直接套用**；加大小上限與逾時。關鍵是保留「套用前看得到」。

### 成長池拆成世界＋台股（2026-09-14 評估，使用者決定維持兩檔）
`session: claude-d8 [b4b834]`

構想：退休5 的成長池由 009826 90% 改成 **009826 60% ＋ 009816 30%**，緩衝池 00865B 10% 不變。
**使用者決定不做，理由是維持兩檔的簡潔。** 沒有動任何程式。

**先講資料限制：009816 也是新的**（2026-01-23 上市，0.6 年），跟 009826 一樣量不了。
以下用 **0050 代理台股、VT 代理世界股票**，2010-01～2026-08 的月報酬。
**這是歷史區間量測，不是 40 年 bootstrap**（Codex 那套沒跑）。

| 成長池 | 年化 | 波動 | 最大回撤 |
|---|---|---|---|
| 100% 世界（現行） | 10.50% | 13.30% | −22.27% |
| 2:1 世界:台股（本案） | **12.65%** | 13.41% | **−21.12%** |
| 100% 台股 | 16.17% | 18.71% | −29.19% |

多 2.15pp 報酬、波動幾乎不變、回撤還略淺。**數字面是划算的。**

**量測時踩到的坑：日報酬相關只有 0.113，那是跨時區失真**（第四十四次）。
月報酬相關 **0.557** 才是真的。用日相關去算分散效果會嚴重高估。

**緩衝池機制不受影響**：均衡佔比 9.65% → 9.07%，起始的 10% 仍在均衡點附近，
自我平衡與免再平衡的性質都還在。規則對成長池裡裝什麼是中性的。

**但成長池內部會漂，這是真正的問題。** 規則只從成長池撥 4% 出來，
**從不平衡成長池內部**。台股佔成長池的比例實測：

`33.3%（起始）→ 27.3%（2013）→ 31.5%（2017）→ 38.9%（2021）→ 53.5%（2026）`

**十六年把 60/30 變成約 45/45。** 想用 30% 框住台股曝險，但沒有人在維持那個框——
與退休1 的正二權重漂移是同一個問題。再加上 0050 超過一半集中在台積電，
起始就有整體 15% 以上押在單一公司，漂移後更多。

**數字看不到的那一面**（第四十一次的論點，在此組合下仍成立但程度較輕）：
台海若出事，台股與台幣同時重挫，而世界股票以台幣計價會因台幣貶值而上漲。
**那個情境在 2010–2026 樣本裡一次都沒發生**，而這段期間又是台積電／AI 的超級循環，
沒有任何台灣本地危機。16.17% 不是預測。

**若日後要做，兩件事要先解決**：跑 40 年 bootstrap 才看得到尾部（這次只有一段歷史）；
以及**先決定成長池內部要不要定期再平衡**——不決定的話「60/30」只是起始條件，不是長期持有的東西。

### 再平衡策略選項（2026-09-11 評估，使用者決定暫不實作）

構想：側邊欄配置比重下方提供再平衡觸發條件（永不執行／總報酬超過 N 萬／
總資產變動超過 N 萬／其他）。**評估後因改動範圍與價值不成比例而擱置。**

**若日後要做，先解決這兩個問題：**

**1. 金額型觸發會在四個頁面靜默失效。** 側邊欄的「總投入金額」是**選填**
（`value=None`，不填則維持百分比分析）。五個分頁中只有「投資報酬」需要金額，
相關性、Beta、夏普、AI 深度解讀全部以百分比運作。採金額門檻時，使用者沒填金額
就等於設了沒作用 —— 比沒有這個功能更糟。
**業界標準的偏離門檻（band rebalancing，任一標的偏離目標 ±N 個百分點）不需要金額**，
五個頁面都能用，也更貼近再平衡的本意。建議：定期 + 偏離門檻為主，
金額型只在有填金額時才可選，否則明確停用並說明原因。

**2. 不計成本的再平衡會系統性高估效果。** 全 app 現標明「未計交易成本與稅金」；
買進持有不交易，這個簡化沒問題。但再平衡會產生實際賣出，台股賣出有證交稅 0.3%
加手續費。門檻越敏感、觸發越頻繁，回測就越比現實好看 —— 這是回測的經典陷阱。
要做就得加可調的成本假設（建議預設 0.4%）。

**改動範圍（已查證）：**

- 計算組合淨值的地方 **6 處**：`correlation.py:27`、`sharpe_analysis.py:16`、
  `beta_analysis.py:56`、`investment.py:61`、`investment.py:112`、`insights.py:205`
- 寫著「不再平衡」的文案 **13 處**（6 個 .py 檔 + `about.md` 5 處）
- `investment.py` 的權重漂移追蹤圖，docstring 寫著 *"preserving buy-and-hold drift"* ——
  **那張圖存在的目的就是呈現不再平衡造成的漂移**，加了再平衡後必須同時標出
  再平衡時點，否則圖會難以解讀

**建議實作方式**：先寫純函式 `rebalanced_nav(segment, weights, rule)` 回傳淨值序列
與再平衡事件日期，可毫秒級單元測試（符合本專案「核心計算不 import streamlit」的架構原則），
再接到那 6 個地方。

---

### 雲端排程監控 APP（2026-09-15 實作後停用，環境擋住）
`session: claude-09 [a371a6]`

使用者要一個 Claude Routine 定期檢查正式站的功能與數據。**做出來了，跑過一次，然後停用**——
routine id `trig_01MzL6fnKLXPmotnQx9Q86To`，`enabled: false`，設定還在，想恢復只要打開開關。

**兩道牆，都不是程式問題：**

**1. 雲端沙箱的出網政策擋掉 Yahoo。** 實跑證據：clone ✅、venv ✅、pip ✅、
`pytest` **154 passed** ✅，然後 `smoke_live.py` 全滅。那個 agent 去查了沙箱自己的
proxy 狀態端點（`http://127.0.0.1:39129/__agentproxy/status`），
`fc.yahoo.com`、`guce.yahoo.com`、`query2.finance.yahoo.com` 的 CONNECT 一律 **403
policy denial**，`recentRelayFailures` 全是 `connect_rejected`。不是限流、不是斷線。

**這一點讓整個構想失效，理由很具體：今天那個 NaN 缺陷，pytest 從頭到尾都是綠的。**
154 項單元測試一項都沒紅，因為它們不打網路。所以一個只跑得動 pytest 的監控，
會在「每一檔都取不到股價」的時候回報正常——**比沒有監控更糟**。

**2. 正式站是登入制的，沒有可匿名探測的端點。** 未登入時任何路徑都 303 轉到
`share.streamlit.io/-/auth/app`，`/_stcore/health` 也一樣。外部監控只看得到那個轉址，
**無論 app 是好的、壞的還是睡著的**。

**順帶回答一個問過的問題：留著 routine 不會避免雲端 APP 休眠。** 它根本沒碰那個網址；
就算加一行去 ping，請求也死在認證閘道、到不了 app 容器。要靠訪問保活只有兩條路：
帶登入憑證（等於把帳號憑證放進 routine，不建議），或把 app 改成公開——
**但公開之後任何人都能點 AI 分頁燒掉你自己的 HF 額度**（每月只有 $0.10），
那多半正是當初設登入限制的原因。

**一個意外的好消息：雲端 routine 可以推播到手機的 Claude APP。** 那次執行自己用了
`PushNotification`，回傳 `Mobile push requested.`（00:59:49 UTC）。
**沒有確認過使用者是否真的收到**，只知道請求送出去了。

**若日後要恢復，先解決的順序是**：(a) 能不能讓雲端環境放行 Yahoo 那幾個網域；
(b) 若不能，就把檢查搬到本機排程（那裡實測 39/39 通過），雲端 routine 只保留
「程式碼有沒有被改壞」這個弱得多的角色，而且**回報裡必須明講它看不到資料層**。

## 變更紀錄

> 由舊到新，**新的一則加在這個檔案的最後面**。

### 2026-09-11 —— AI 解讀獨立成分頁 + 修掉三個真實缺陷
`session: claude-d1 [e01f5e]`

**新增「AI 深度解讀」分頁**（在「投資報酬」之後、「關於與使用說明」之前）。

關鍵設計：**新分頁必須自給自足。** 這個 app 用 `tab.open` 做延遲渲染，
而 AI 面板原本寄生在「相關性與分散效果」分頁算出來的 `volatility` / `drawdown` 上 ——
使用者如果從沒點開那一頁，資料根本不存在。
所以把組合統計抽成純函式 `correlation.portfolio_stats()`，AI 分頁自己算自己要的。

「數據解讀」的文字結論**刻意留在相關性分頁**，因為它是緊貼上方圖表的說明，
搬走會讓那頁少一塊。AI 分頁有自己的配置摘要、期間、指標卡，
外加一個可展開的「送出的統計摘要」，讓使用者看得到 AI 到底收到哪些數字。

**修掉三個缺陷**（都是實測才浮出來的，看程式碼看不出來）：

1. **推理模型會讓功能永久顯示「AI 暫時無法回應」。**
   原本只讀 `content`，空字串就 `raise ValueError('Empty response')`，
   被 `ai_panel` 的 `except` 接住後顯示籠統錯誤。
   使用者會以為是網路或額度問題，其實是模型選錯，而且完全沒有線索。
   現在偵測到 `content` 空但 `reasoning_content` 有內容時，
   拋 `ModelOutputError` 並說明原因。

2. **讀取逾時 30 秒太短。** 這是使用者實際撞到的錯誤。
   `:cheapest` 路由實測要 40.2 秒。已改 90 秒（`READ_TIMEOUT_SECONDS`）。

3. **`max_tokens` 700 會把回答切在半句。**
   `:fastest` 實測 `out=700` 剛好撞上限。已改 1100，
   並加上截斷偵測：`finish_reason == 'length'` 時在文末標註「內容可能未完整」。
   財經文字講一半卻看起來像完整結論，比直接失敗更危險。

**順手**：`module_compat.load_renderer` 原本只在「函式少了新參數」時重載，
但雲端可能整個函式都還沒有，那會直接 AttributeError。改成 `function is None` 也重載。

**測試**：新增 9 個（組合統計加權正確性、payload 期間欄位、相關配對去重與 NaN、
推理模型錯誤路徑、截斷標記、模組重載）。29 → 38 passed。
原本寫死 `timeout == (5,30)` 的斷言改成引用常數，以後調參數不會再假性失敗。

**走過的彎路**：一開始推薦 `:cheapest` 是只看單價、沒考慮延遲，
在互動式 app 裡是錯的取捨。另外一度以為「GLM-4.7-Flash 預設不 thinking」是模型屬性，
實測後發現那只在特定供應商成立。**涉及供應商行為的結論一定要實測，不要靠一次觀察外推。**

### 2026-09-11（第二次）—— prompt 改為產出評論，並標示產生模型
`session: claude-d1 [e01f5e]`

**問題**：第一版上線後實測，模型只是把 payload 條列複述，不帶觀點。
而且複述時出錯：把「組合年化波動」和「加權個別波動平均」當成同一個數字，
又把「較 N/M 檔標的淺」寫成讀起來像比例。**模型沒有理解數字的角色，只是搬運。**

**改法**：system prompt 拉成 `SYSTEM_PROMPT` 常數並重寫，要求輸出三段二級標題
（這個配置的樣貌／值得注意的風險／可以考慮的方向），明文要求「不要逐條複述數字」，
並針對上述兩個誤讀寫了防呆規則。允許模型運用對標的所屬產業的一般認識討論集中度，
但數值一律只能引用輸入。

**法規分界（刻意保留的禁止項）**：具體買賣指令與時點、個股推薦、報酬或走勢預測、
保證性用語。台灣的投資顧問是特許業務，配置層面的觀察可以，買賣指令不行。
頁尾免責也改成「教育性資訊，不構成投資建議，也不是個人化理財規劃」。

**顯示模型**：session 快取從存字串改為存 `{'text', 'model'}`，
輸出下方標示實際產生的模型 ID。使用者要求的，也方便比較不同模型的效果。

**模型選擇覆核（實測，同一 prompt 與 payload）**：

| 模型 | 耗時 | tok/s | 品質 |
|---|---|---|---|
| `GLM-4.7-Flash:fastest` | 12–15s | 25–37 | ✅ 乾淨繁中、無捏造 |
| `Qwen3.5-35B-A3B:fastest` | 3.3–3.8s | 132 | ❌ 見下 |

Qwen 快 4 倍但**不能用**：出現簡體字「负相關」（prompt 明文禁止仍漏）、
混入英文「該 sector」、捏造「透過調整權重」（根本沒有這回事）、
語意不通的句子、並把 prompt 的免責原文照抄進輸出。
**財經文字出現簡體字與捏造事實，比慢 10 秒嚴重得多 —— 維持 GLM。**

另註：GLM 曾出現一次 70 秒的離群值（供應商打嗝），連測三次穩定在 12–15 秒。
90 秒逾時的設定有足夠緩衝，不要調低。

### 2026-09-11（第三次）—— 專業分析框架 prompt + 補齊 payload
`session: claude-d1 [e01f5e]`

**判斷**：改 prompt 只是一半。原本 payload 只有三句結論、相關配對、比重、兩個期間 ——
分析師拿到三個數字也只能講通則。**瓶頸在資料不在措辭。**

新增純函式 `allocation_facts()`，把專業配置檢視需要的數字全部在 Python 算好：

- 每檔的比重／區間報酬／年化波動／最大回撤（依比重由大到小排序）
- 集中度：最大單一比重、前二大合計、HHI、**有效持股檔數**（1/HHI）、實際檔數
- 組合：區間報酬、年化波動、加權個別波動、**波動差距（百分點）**、
  最大回撤、**回撤回復所需漲幅**

「回撤回復所需漲幅」特別重要：跌 20.5% 要漲 25.8% 才回得來。
這個不對稱性是專業檢視的核心概念，**但它是除法** —— 一定要在 Python 算好再給，
不能讓模型自己算。

**順手修掉一個既有缺陷**：payload 裡 `weights` 用代碼（`0050.TW`）、`pairs` 用中文名，
模型很難把兩邊對起來。現在統一用顯示名稱。
（實作時踩到自己的坑：`rename(index=label)` 會把 `等權重組合` 也套上 `label()`
變成「等權重組合 名稱暫未取得」，害 `volatility[portfolio_name]` KeyError。
改用字典映射，只換持股不動組合列。）

**SYSTEM_PROMPT 重寫成四段分析框架**：配置結構／分散效果檢驗／風險特徵／權衡與方向。

**逐句檢查輸出後，補了三條防呆規則**（每一條都是實測抓到的真實錯誤，不是預防性的）：

1. **回撤不對稱不可寫成「僅需」。** 模型曾寫「僅需上漲 25.8% 即可回到原點」——
   把不對稱性講反了，重點是「必須漲更多」。
2. **代價必須與方向邏輯一致。** 模型曾寫「引入低相關資產（如債券）但這會**增加**
   對半導體的曝險」—— 完全相反，是為了湊「代價」硬掰的。
3. **「有效持股檔數」只衡量比重，不衡量相關性。** 模型曾寫「有效檔數等於實際檔數，
   代表分散完全未發揮作用」—— 講反了，兩者相等代表比重完全平均，是比重層面最分散的狀態。
   分散有沒有效要看相關性與波動差距。

**教訓：加了規則之後三個錯誤都消失，但這是逐句人工核對才抓到的。
改 prompt 之後一定要實際跑一次並逐句檢查，不能只看「讀起來很專業」。**

**測試**：新增 5 個涵蓋 HHI、有效檔數、回復漲幅必大於跌幅、波動差距、排序。38 → 43 passed。

**延遲**：9–36 秒，變異很大（同樣輸入量過 8.9s / 20.7s / 36.4s）。
90 秒逾時仍有緩衝，但如果之後覺得等太久，正解是改用串流輸出（`st.write_stream`），
不是縮短 prompt。

**還沒做**：仍缺投資年期／每月可投入／目標金額／風險承受度（app 根本沒有輸入欄位），
沒有這些就只能給「配置評論」，給不出「理財規劃建議」。

### 2026-09-11（第四次）—— 串上夏普、改為串流輸出
`session: claude-d1 [e01f5e]`

**夏普已餵給 AI，Beta 與配息還沒。** 三者成本差很多：

| 項目 | 現況 | 成本 |
|---|---|---|
| 夏普 | `sharpe_stats()` 是純函式、零網路 | 幾乎免費，**已完成** |
| Beta | `fit_beta()` 純函式，但要抓 `^TWII` 基準 | 多一個網路請求與失敗路徑 |
| 配息 | 每檔都要 `load_distributions()` | 最重，N 個網路請求 |

**設計考量**：AI 分頁目前不觸發任何額外網路請求，只用已載入的行情。
配息原本被刻意放在 `st.toggle('載入配息試算')` 後面就是為了避免自動抓取。
硬串進來會讓開啟 AI 分頁變慢很多，違背這個專案的延遲載入設計。
若要做，建議設計成**選用勾選**（「一併分析 Beta 與配息（需額外抓取資料）」），
讓分頁預設保持快速。

新增 `sharpe_facts()`。無風險利率從 `st.session_state['sharpe_rate']` 讀，
但因為延遲渲染，使用者可能從沒開過夏普分頁 —— 預設 `DEFAULT_RISK_FREE_RATE = 2.0`
並在 payload 裡標明假設值。整段包在 try 裡：夏普是加分項，不該擋住其餘解讀。

prompt 新增防呆規則：**夏普為負代表低於無風險利率，不等於虧損**（最常見的誤讀），
且組合夏普不是各檔夏普的加權平均。實測輸出遵守了。

---

**改成串流輸出（`st.write_stream`）。** 這是為了解決一個已經咬到的問題：
加了 payload 之後出現一次 **>90 秒逾時**，整個功能失敗。

| 方式 | 首字 | 總時間 |
|---|---|---|
| 非串流 | 全程空白 | 8.4–11.7s，偶發 >90s ❌ |
| 串流 | **1–10s** | 8.9–20.4s |

串流同時解決兩件事：使用者馬上看到文字開始跑，而且
**讀取逾時變成「chunk 之間」而非總生成時間** —— 慢供應商不再造成整體失敗。
延遲變異是供應商端的，縮短 prompt 犧牲品質並不能解決，串流才是正解。

實作上 `request_insight`（緩衝版）保留給腳本檢查，UI 走 `stream_insight`。
共用的請求組裝與空回應判斷抽成 `_body()` / `_guard()`，避免兩條路徑各自長歪。
`ai_panel` 串流完成後把結果寫進 session 快取，**並刻意不再 `st.markdown` 一次**
（文字已經在畫面上，重畫會變兩份）。

**測試**：新增 4 個串流測試，含「畸形 chunk 不可中斷整段解讀」——
SSE 解析是新的失敗面，不能沒有測試守著。43 → 48 passed。

### 2026-09-11（第五次）—— 修好 test_app.py 的 6 個紅燈
`session: claude-d1 [e01f5e]`

根因是 `99b5a42` 的 `preset_picker()` 在首次載入就套用「衝刺」配置，
把選股從測試 patch 的 `DEFAULT_SYMBOLS`（10 檔）換成預設配置的 3 檔。

**還有第二個被遮住的破口**：預設配置的下拉選單插在側邊欄最前面，
`selectbox[0]` 從「價格基準」變成「預設配置」，所以
`app.selectbox[0].select("收盤價（未還原）")` 也早就壞了 ——
只是測試在更前面的斷言就失敗，看不到這一個。

**三個修法**：

1. `new_app()` 預設設 `portfolio_presets_initialized=True` 跳過預設配置，
   讓每個測試自己決定標的。這些測試本來就不是在測預設配置，
   是預設配置干擾了它們的前置設定。
2. 新增 `pick(app, kind, label)` 用**標籤**查找 widget 取代索引。
   索引查找只要上方插入任何 widget 就會壞 —— 這次就是這樣壞的。
3. 新增 `test_preset_applies_on_first_load`，真正測預設配置的行為
   （首次載入套用衝刺 3 檔 + 1000 萬、切換到退休變 4 檔 + 3000 萬）。
   **預設配置原本完全沒有測試，這正是它當初改壞別的測試卻沒人發現的原因。**

新測試做過變異驗證：把衝刺的 `2454.TW` 改成 `2603.TW`，測試如預期失敗。

48 → 55 passed，全綠。

### 2026-09-11（第六次）—— Beta 與配息串進 AI（一律送出，不設選項）
`session: claude-d1 [e01f5e]`

使用者要求不做選用勾選。折衷做法：**資料在按下按鈕時才抓，不是開啟分頁時**——
不用勾選，開分頁依然秒開，只有真的要 AI 分析才付網路成本。
`ai_panel(payload, expand)` 的 `expand` 是個 callable，在按鈕分支裡才執行；
`payload` 基礎版仍即時產生，供快取鍵與透明度展開區使用。

**成本我原本高估了。** 實測冷快取（用從未抓過的標的、全新行程）只要 **0.4 秒**，
不是我擔心的數秒級。yfinance 的配息端點很快，`load_distributions` 又有 1 小時快取。

新增 `beta_facts()`、`dividend_facts()`，以及 `market_and_income_facts()` 把兩者
**各自獨立 try**：一邊掛掉不能連累另一邊，缺哪邊就在 payload 放 `資料缺漏` 欄位，
並要求模型明講該面向無法取得而不是略過（prompt 規則 11）。

**踩到一個假象**：基準預設是 `0050.TW`，而它常常同時是持股。
對自己回歸必然得到 Beta 1.00、R2 1.00，模型會誤讀成「完美追蹤大盤」。
payload 現在會在這種情況下加一個 `注意` 欄位明講這兩個數字不具意義。

**指標有鑑別力**（兩個組合實測對照）：

| | 衝刺（0050+2330+2454） | 高股息（0056+00878+00919） |
|---|---|---|
| 零配息月份 | 8 / 14 | 1 / 14 |
| 單月最高佔全期 | 45.5% | 12.3% |
| 組合 Beta | 1.16（R2 0.80） | 0.52（R2 0.62） |

**新增兩條防呆規則**（同樣都是實測抓到的真實錯誤）：

- **Beta / 配息的典型誤讀**：低 Beta 不等於低風險；R2 低時不可用 Beta 下判斷；
  除息不是獲利、配息率不可與報酬率相加；除息日不是付款日。
- **不得使用數字不支持的程度用語**。模型曾把 -20.6% 的回撤寫成「價格腰斬」——
  腰斬是 -50%。現在禁止「腰斬」「崩盤」「重挫」這類自行加碼的嚴重程度。

加規則後輸出品質明顯提升，出現了這種真正有用的判斷：
「組合夏普比率 2.69，略低於 0050 元大台灣50 的 2.72，顯示每承受一單位波動
所換取的超額報酬並未優於直接持有指數。」—— 挑三檔電子股還不如直接買 0050。

**測試**：新增 3 個失敗隔離測試（一邊掛不能連累另一邊、兩邊都掛要明講、
都正常時不可出現缺漏欄位）。55 → 58 passed。

### 2026-09-11（第七次）—— 連 prompt 一起公開
`session: claude-d1 [e01f5e]`

原本展開區只顯示數字，改成顯示**送給 AI 的完整內容**：指示（system prompt）與數字兩部分。
理由是使用者有權知道 AI 被要求用什麼角度解讀、哪些話不准講 ——
只公開數字而不公開指示，透明度只做了一半。

用 `st.code(SYSTEM_PROMPT, language=None, wrap_lines=True, height=320)` 原文顯示，
不用 `st.markdown`：prompt 裡有 `##` 標題，渲染後會跟 AI 的實際輸出混淆，
而且使用者要看的是「模型收到的原文」，不是排版後的樣子。附帶複製按鈕方便外部比對。

新增 `test_ai_tab_shows_the_prompt_not_just_the_numbers` 鎖住這個性質，
避免日後重構讓 prompt 悄悄從畫面消失。
做過變異驗證：把 `st.code(...)` 換成 `pass`，測試如預期失敗。58 → 59 passed。

### 2026-09-11（第八次）—— 指示可編輯、標示模型、加上 HF 標記
`session: claude-d1 [e01f5e]`

**指示改成可編輯**（`st.text_area` + 套用／恢復預設，存在 `session_state['system_prompt']`）。

**必須一起處理的地雷：快取鍵要含 prompt。** 原本 key 是 `sha256(model + payload)`，
prompt 改了但 payload 沒變就會直接回舊快取，看起來像「改了沒用」。
現在是 `sha256(model + prompt + payload)`。`SYSTEM_PROMPT` 也從模組常數改成
`request_insight` / `stream_insight` 的參數。

**安全取捨**：預設指示裡有防止模型自行計算、誇大幅度、給出買賣指令的規則。
使用者移除後這些限制就沒了，所以自訂時畫面會出現警告，
產出文字下方也標示「使用自訂指示」，避免跟預設指示產出的內容混為一談。
修改只存在當次連線，不寫入任何檔案，不影響其他使用者。

**修掉一個設計缺陷**：透明度區塊原本在 token 檢查**之後**，
所以沒設定 AI 時完全看不到「啟用後會送出什麼」。現在移到檢查之前，
未設定模型也看得到指示與數字（也因此 AppTest 不需要 secrets 就能測）。

**模型說明**：新增 `MODEL_NOTES`，以模型代號（去掉 `:fastest` 後綴）對應簡短說明。
**內容只寫本專案實測到的結果**，不抄廠商宣稱 —— 例如 Qwen 那則直接寫明
「實測會出現簡體字、夾雜英文並捏造事實，因此未採用」，讓後人知道為什麼不選它。

**HF 標記**用 🤗 emoji，不嵌入官方 logo 圖檔：這就是 Hugging Face 自己的品牌符號，
不必為此增加二進位資產與授權考量。

**測試**：新增 4 個（編輯套用與還原的往返、自訂 prompt 有送進請求、
未指定時用預設、`model_note` 要忽略路由後綴）。59 → 63 passed。

### 2026-09-11（第九次）—— 移除 AI 分頁上重複的指標與結論
`session: claude-d1 [e01f5e]`

使用者指出組合三張指標卡與三行文字結論在這一頁是多餘的 —— 它們在
「相關性與分散效果」分頁已經有了，擺在這裡只是把真正的 AI 內容往下推。

兩者現在只作為 payload 材料存在（`conclusions()` 仍照常呼叫、`portfolio_stats()`
的結果仍送進 `allocation_facts()`），**只是不再渲染**。
分頁保留配置與期間兩行 caption 當作「現在看的是什麼」的脈絡，其餘直接進 AI 面板。

### 2026-09-11（第十次）—— 自訂指示「沒生效」的真正原因
`session: claude-d1 [e01f5e]`

使用者在 prompt 末尾加「## 長度／上限 5 字」測試，回報沒生效。實測三種情況：

| 測試 | 輸出長度 |
|---|---|
| 原始 prompt（對照） | 705 字 |
| 原始 + 末尾加「上限 5 字」 | **712 字**（完全無視） |
| 只留「上限 5 字」，其餘刪掉 | **10 字**（「組合波動 37.2%」） |

**功能沒壞** —— 第三種證明自訂 prompt 確實有送進請求。
問題是**指示衝突**：預設 prompt 有 2,000 字，要求四段、每段 3–5 句、
還規定每段講什麼。末尾附加一句「上限 5 字」與整份要求正面牴觸，
模型服從份量較重、較具體的那一邊。

**但這也暴露一個真實的 UX 缺陷**：只在文字框打字而沒按「套用」時，畫面完全沒有提示，
所以無法排除使用者其實沒套用。現在 draft 與已套用的指示不同時會顯示
「編輯內容尚未套用」。展開區也加了一行說明，講清楚附加規則常常會被既有段落壓過，
要生效多半得改寫或刪掉衝突的部分，而不是往後面加。

新增 `test_unapplied_edit_says_so`。63 → 64 passed。

### 2026-09-11（第十一次）—— 版面整理：分頁分組、圖表標籤、期間提示
`session: claude-d1 [e01f5e]`

**四個分頁合併為「風險分析」群組。** 相關性與分散效果、Beta、夏普回答的是同一類問題
（這個配置的風險長什麼樣），原本一個在最上層、兩個藏在「指標分析」底下。
頂層 6 個減為 5 個。相關性排第一，開啟群組即直接顯示，**點擊數沒有增加**。
父分頁改名「風險分析」，因為「指標分析」太籠統（報酬率、配息率也是指標卻在別頁）。
子分頁 key 從 `indicator_tabs` 改為 `risk_tabs`（選項變了，沿用舊 key 會記住不存在的選擇）。

夏普的標題去掉「夏普分析｜」前綴，與另外兩頁一致 —— 子分頁標籤已經寫了一次。

**三張水平長條圖的 Y 軸標籤會被吃掉。** 使用者在夏普那張發現第 2、3 列標籤消失，
隔一個掉一個 —— 這是 Vega 判定重疊時的隔列剔除，不是單純空間不夠。
**三張圖都有這個問題**（夏普、Beta、年化波動比較），只有相關性熱圖本來就設了
`labelOverlap=False`。三張統一加上該設定，每列高度也統一為 `ROW_HEIGHT = 44`
（原本 30/34/38 各不相同）。`sharpe_analysis.UI_VERSION` 推到 3，
`streamlit_app.py` 的重載判斷同步改成 `< 3`（改版本號卻不改判斷，機制就失效）。

**期間提示改成外層顯示一個。** 使用者反映紅框散落各頁重複。
但實測證明四個頁面的期間**不相同**，全部收成一個會顯示錯的資訊：

以「退休」配置實測（009816.TW 最早只到 2026/01/23，另外三檔有 2025/09/09）：

```
側邊欄選的    2025/09/09 — 2026/09/09
實際共同期間  2026/01/23 — 2026/09/09   152 個交易日
相關性期間    2026/01/26 — 2026/09/09   151 筆   ← 日報酬少一天，起日晚一個交易日
組合試算期間  2026/01/23 — 2026/09/09   152 個
```

做法：新增 `app/ui.py` 的 `period_banner()`（樣式取自 `investment.py` 原本手寫的
HTML 紅框，抽成共用避免各自飄移），用 `st.empty()` 在 `st.tabs()` **之前**保留插槽，
資料載入後回填 —— 紅框因此位於分頁列上方，每個分頁都看得到但只有一個。
各頁的期間退回普通 caption，保留差異但不搶眼。

紅框還會在實際期間比使用者選的短時加一句「你選擇 X 起，但部分標的資料較晚，已自動縮短」。
以前使用者得自己發現「怎麼只有 152 天」。

**順手修掉 README 兩個錯誤**：部署說明是逐檔列舉且早已漏掉多個模組（照做會在雲端
ImportError），改成「推整個 repository」；逾時仍寫 30 秒，已改為 90 秒並說明是串流。
`investment.py` 的 `from html import escape` 改用共用元件後沒人使用，一併移除。

**測試**：新增 3 個（分頁結構與預設子頁、期間框只有一個、缺資料時出現縮短提示）。
65 → 66 passed。

### 2026-09-11（第十二次）—— 截斷提示頻繁出現：上限剛好卡在自然輸出長度
`session: claude-d1 [e01f5e]`

使用者回報常看到「（輸出已達長度上限，內容可能未完整。）」。實測（退休配置、
payload 3533 bytes、含夏普＋Beta＋配息）：

| max_tokens | 實際輸出 | finish_reason |
|---|---|---|
| **1100** | **1100** | **length（截斷）** |
| 2400 | **1099** | stop（完整） |

**模型自然想寫的長度就是約 1099 tokens，上限剛好卡在 1100 —— 差一個 token。**
只要輸出稍有變動就截斷，所以幾乎每次都會看到提示。

原因：`MAX_OUTPUT_TOKENS = 1100` 是在 prompt 只有四段、payload 還沒有夏普／Beta／配息
時設的。後來 prompt 增為五段、payload 多了三組資料，輸出自然變長，上限卻沒跟著調。
**改設定時要回頭檢查跟它相關的舊上限。**

改為 2400。**HF 按實際產生的 token 計費而非按上限，所以這個 headroom 不花錢。**

另記：payload 變大後延遲明顯上升（48–64 秒，先前是 9–20 秒）。串流讓使用者
看得到進度所以還可接受；若要縮短，正解是把 prompt 的「每段 3–5 句」收緊，
而不是調低上限（調低只會回到截斷）。

### 2026-09-11（第十三次）—— 修掉波動差距的因果錯誤；句數指示無效（實測）
`session: claude-d1 [e01f5e]`

**修好的**：模型曾寫「組合波動低於加權平均…但這是由於標的間的**波動率差異**產生的結果，
**而非相關性結構**帶來的保護」——**數學上講反了**。組合波動低於加權個別波動的唯一來源
就是相關係數小於 1；若所有相關係數都是 1.0，兩者會恰好相等。波動率差異本身不產生任何
降低效果。新增 prompt 規則 13 講明這件事，實測兩次都不再出現錯誤用語，
改為正確的「相關性高於 0.76…直接導致了 0.8 個百分點的波動差距」。

**沒效果的**：把「每段 3–5 句」收緊為「2–4 句」，想縮短輸出以降低延遲。**實測無效**：

| prompt | tokens | 字數 |
|---|---|---|
| 3–5 句（原本） | 1099 | 1225 |
| 2–4 句 #1 | 1036 | 1179 |
| 2–4 句 #2 | **1242** | **1405** |

第二次反而更長。**模型照自己的習慣寫，句數指示沒讓它變短。** 指示保留（無害），
但不要以為它能控制長度。

**不要誤判延遲來源**：同一次測試延遲從 48–64 秒降到 17–19 秒，但換算吞吐量是
17 tok/s vs 61 tok/s ——**那是供應商當下的負載差異，不是輸出變短造成的**。
同樣的 payload 在不同時間跑會差三倍。想靠縮短輸出來降低延遲，效果遠小於供應商變異。

順帶驗證了上一次調高上限的必要性：第二次輸出 1242 tokens，在舊的 1100 上限下會被截斷。

### 2026-09-11（第十四次）—— 「零配息月份」誤導模型說出錯誤結論
`session: claude-d1 [e01f5e]`

使用者回報 AI 對退休配置的 00984B 寫出「9 個涵蓋月份中有 2 個月零配息…
現金流並不穩定，無法提供持續的平均收入」。查證後發現**兩個零配息月份都是統計假象**：

```
2026-01  0 次除息  ← 試算期間自 01/23 起，這個月只涵蓋 9 天
2026-07  2 次除息  ← 07/02 與 07/31
2026-08  0 次除息  ← 07/31 那次本來屬於 8 月，除息日往前漂了一天
全期 8 次除息，約 7.5 個月 —— 這是穩定的月配
```

**是 payload 結構誤導了模型，不是模型亂講。** 送出去的只有「零配息月份數 = 2」，
照字面解讀必然得出「現金流不穩定」。

**修法以資料為主、prompt 為輔：**

payload 的配息欄位改成：`全期除息次數`、`每月除息次數`（逐月計數）、
`不完整月份與涵蓋天數`、`完整月份數`、`完整月份中零除息數`，各標的也加上 `除息次數`。
月份邊界的判斷抽成純函式 `monthly_pattern()`（這種邊界邏輯最容易寫錯又最難察覺），
配 4 個單元測試：首尾不完整月份要標示、除息日漂移要呈現為「某月 2 次、次月 0 次」、
完整月份不可有 partial 條目、無事件時仍列出每個月。

prompt 新增規則 11：判斷配息規律性依據除息次數而非零配息月份；不完整月份與邊界漂移
不算中斷；「9 個月除息 8 次」就是穩定月配。

**實測修復後**：「全期除息次數為 8 次，7 個完整月份中有 6 個月都有除息動作，
顯示配息頻率穩定。雖然 2026-08 月份為零除息，但這是由於除息日恰好在月份邊界，
並非配息中斷。」

**教訓**：模型的錯誤結論常常源自 payload 給了容易誤讀的聚合數字。
加防呆規則之前先問「這個欄位單獨看會不會被誤解」——**能改資料就別只改 prompt**。

順帶一提：驗證用的字串比對把「並非配息中斷」誤判為錯誤用語（否定句）。
自動檢查只能當初篩，結論仍要人看過。

66 → 70 passed。

### 2026-09-11（第十五次）—— 額度用完與推理模式失控：把失敗講清楚
`session: claude-d1 [e01f5e]`

使用者回報畫面出現「模型把 2400 個輸出額度全部用在推理，沒有產生正文」。查證後
發現**兩個獨立問題同時發生**，而且都不是程式改壞（當時測試 71 passed 全綠）：

**1. `:fastest` 開始忽略 `enable_thinking: False`。** 連測四次全部
`正文 0 / 推理 517–662 / finish=length`。同一個參數在兩天前是有效的 ——
**供應商端行為變了**。後綴 `:fastest` 只保證挑最快的供應商，不保證那家支援這個參數。
另測 `reasoning_effort: none` 也無效。當時可用的是 `zai-org/GLM-4.7`（非 Flash）
與 `Qwen/Qwen3.5-35B-A3B`。

**2. 每月免費額度（$0.10）用完，回 HTTP 402。** 主要是這一天大量測試燒掉的：
prompt 調校、模型比較、截斷測量、配息驗證約四五十次，加上 120 檔 ETF 的嵌入測試。

**修掉的真實缺陷**：402 原本被 `raise_for_status()` 變成 `HTTPError`，
落進通用的 `except requests.RequestException`，畫面顯示「AI 暫時無法回應，**請稍後重試**」。
**那是錯的建議** —— 額度用完重試一百次也不會好。現在 `check_response()` 把狀態碼
對應到可行動的說明：401/403 金鑰問題、402 額度用完（含帳單頁）、429 限流、5xx 才叫人重試。
例外改成 `ReadableError` 基底（`ModelOutputError` 與 `ServiceError` 繼承它），
`ai_panel` 攔它並原文顯示。

**測試**：新增 4 個（402 不可讀成暫時性失敗、401/403/429 要能區分、5xx 仍建議重試、
200 不受影響）。既有 11 個測試一度失敗，原因是舊 mock 沒有 `status_code` ——
**mock 不夠真實，不是程式錯**，補上後全綠。70 → 75 passed。

**環境雷（新）**：git 簽出後工作檔是 CRLF。用 Python 多行字串比對來 patch 時，
`read_text()` 會統一成 LF 所以仍可匹配，但**在 bash heredoc 裡寫含三引號的 Python**
字串容易被跳脫吃掉。改用 `chr(34)*3` 與 `chr(10)` 組字串最保險。

### 2026-09-11（第十六次）—— 模型改為 GLM-4.7（非 Flash），並更正前一則的錯誤判斷
`session: claude-d1 [e01f5e]`

**更正第十五次的結論：額度並沒有用完。** 當時連續多個 HTTP 402 讓我判斷免費額度耗盡，
但稍後同一把 token 直接回 200。**402 是暫時性的**（可能是短時間爆量），不是永久耗盡。
下判斷前應該多等一會兒再驗一次。（402 的訊息處理仍然值得保留，見第十五次。）

**真正的問題是模型與後綴組合。** 以完整 payload（含夏普＋Beta＋配息）實測：

| 模型 | 首字 | 總時間 | 字數 | 段落 | 結果 |
|---|---|---|---|---|---|
| `GLM-4.7-Flash:fastest` | — | — | 0 | — | ❌ 推理吃光額度、正文空白 |
| `GLM-4.7-Flash:cheapest` | 1.7s | **143.5s** | 5662 | **0** | ❌ 極慢、不遵守分段、撞上限截斷 |
| `GLM-4.7:cheapest` | 1.7s | 25.5s | 720 | 5 | ✅ |
| `Qwen3.5-35B-A3B:fastest` | 3.9s | 24.2s | 1195 | 5 | ✅ |
| **`GLM-4.7:fastest`** | **0.8s** | **3.9–4.7s** | 733–908 | 5 | ✅ **採用** |

**拿掉 `-Flash` 之後 `:fastest` 反而快 10 倍**（4.7 秒 vs 先前的 48–143 秒）。
名稱裡的 Flash 不代表實際比較快 —— 那取決於哪些供應商在服務該模型。
`HF_MODEL` 改為 `zai-org/GLM-4.7:fastest`，`MODEL_NOTES` 同步更新並標明
Flash 版不建議使用及原因。

**這次輸出品質是目前最好的**，先前加的防呆規則全部生效：
有效持股檔數只談比重、波動差距歸因於相關係數小於 1、
7 月兩次除息判定為月份邊界漂移而非中斷、配息率與殖利率不可相加。

**教訓**：模型與後綴要一起當成一個組合來測，而且**必須用完整 payload**。
短提示的測試完全看不出 `GLM-4.7-Flash:cheapest` 會跑 143 秒又不遵守格式。

### 2026-09-12（第十七次）—— 完整輸入可複製，讓使用者拿去餵自己的 AI
`session: claude-d1 [e01f5e]`

expander 裡本來就顯示 prompt 和數字，但它們是**分開兩個區塊**，而且 `st.json`
的內容沒辦法一次選取複製。加了一個合併後的 `st.code`（Streamlit 原生附複製鈕），
把指示與 JSON 接成一段可直接貼進任何聊天模型的文字。

**踩到的陷阱：expander 顯示的 payload 不是實際送出的那一份。**
Beta 與配息要另外打網路，設計上是按下「AI 深入解讀」才補。如果複製鈕直接給畫面上
那份，使用者貼到別的 AI 會少掉兩大塊資料，而且**不會有任何提示**——產出的解讀比
App 內淺，卻看不出原因。所以複製區另放一個「取得完整輸入（含 Beta 與配息）」按鈕，
按下才呼叫同一個 `expand()`。結果以 payload 的 hash 當 key 存進 session_state，
配置或期間改了就失效重抓。

指示取 `active_prompt()`，所以使用者改過 prompt 後複製到的是自己那版而非預設版。
這是刻意的——否則「帶著走」的內容會跟畫面上看到的不一致。

JSON 用三個反引號加 json 圍起來，接收端模型才分得清哪裡是指示、哪裡是資料。
測試 `test_portable_input_carries_both_halves_unchanged` 驗證兩半都逐字保留，
且圍起來的部分仍是合法 JSON。測試數 75 → 76。

### 2026-09-12（第十八次）—— 退休配置改為四個變體
`session: claude-d1 [e01f5e]`

原本的「退休」更名為「退休1」，另加退休2／3／4，四個都是 3,000 萬
（使用者只指定比重沒指定金額，沿用原本的數額）。差異在於：
退休2 把 009816 換成 0050，退休3 降債增 00662，退休4 加倍槓桿部位 00685L。
標的本身都是既有清單裡的，沒有引入新代號。

`format_func` 原本把金額寫死成「衝刺→1,000 萬，其餘→3,000 萬」。
多了三個配置之後這種寫法遲早會說謊，改成直接讀 `PRESETS[name]['amount_wan']`，
顯示字串與現況完全一致。

新增 `test_every_preset_is_fully_allocated`：四張手打的比重表，
只要有一位數字打錯就會少配或超配，而 App 照樣跑得起來、不會報錯。
`validate_weights` 擋的是使用者輸入，擋不到寫死在程式裡的表。

重新命名會動到測試裡的 `select('退休')`（`test_allocation.py` 與 `test_app.py` 各一處）。
測試數 76 → 77。

### 2026-09-12（第十九次）—— 共同期間提示只點名真正卡住邊界的那一檔
`session: claude-d1 [e01f5e]`

截圖上四行提示，其中三行是「資料截至 2026/09/10，限制共同迄日」——
同一個日期重複三次，而且那天只是**當日行情還沒發布**，不是資料缺口。
真正要看的「限制共同起日」被淹在裡面。

改成每一側只點名一檔：起日取 `first_valid_index()` 最晚的，
迄日取 `last_valid_index()` 最早的。其餘同一天缺資料的標的不再列出——
它們講的是同一件事，列出來只是把重點推遠。

**尾端只少一天時整行不顯示。** 那是每日發布時差（有一檔已經有當天資料、
其他還沒），不是涵蓋範圍問題。門檻用 `len(available.index > last) > 1`，
所以下市或停止更新那種真的少好幾天的情況仍然會講。

連帶把整個 `st.info` 改成 `reasons` 非空才顯示。原本只要期間有縮短就會出現，
現在若兩側理由都被收掉，剩下的標題只是重複外層期間橫幅已經寫的東西。

新增 `test_common_period_names_only_the_binding_symbol` 重現截圖情境
（兩檔起始日不同、三檔尾端各少一天），斷言只出現最晚起始的那一檔、
且完全沒有迄日那行。測試數 77 → 78。

### 2026-09-12（第二十次）—— 金額欄下方加上「萬」的第二行
`session: claude-d1 [e01f5e]`

八位數的金額掃一眼判斷不出量級，而使用者在側邊欄輸入的本來就是「萬」。
在 `ui.py` 加 `wan()`（純函式，可測）與 `money_metric()`，
所有 NT$ 卡片改走後者。

**第二行用 `st.metric` 的 `delta`**——那是 metric 唯一的副標位置。
必須同時關掉 `delta_arrow` 與 `delta_color`（設 `'off'`），
否則會出現綠色向上箭頭，讀起來像「相對某個先前數字的變化」，完全是誤導。

**低於 1 萬不顯示。** 「0.1 萬」比它上面那個完整數字更難讀，等於幫倒忙。
低於 100 萬保留一位小數（23.5 萬），再往上取整數（4,320 萬）——
在 432 萬這種量級，小數點後那位沒有資訊價值。

套用範圍：總報酬分頁 3 張（初始投入／期末試算價值／區間損益）、
配息分頁 3 張、每月配息 2 張（「零配息月份」是次數不是金額，不加），
以及「最大高點至低點金額差」那行 caption 補上括號。
`區間報酬率` 是百分比，測試明確斷言它**不會**長出萬元行。

表格欄位（投入金額、期末市值等）維持元為單位——dataframe 的儲存格沒有「下方」
可以放第二行，硬塞會變成把兩個數字擠在同一格。

新增 `app/tests/test_ui.py`（第一個 ui 模組測試）與
`test_money_cards_carry_the_wan_reading`。測試數 78 → 81。

### 2026-09-12（第二十一次）—— 區間報酬率卡片附上期間
`session: claude-d1 [e01f5e]`

用的是跟萬元行同一個位置（`delta` 配 `delta_arrow='off'`、`delta_color='off'`），
所以四張卡片第二行的樣式一致：一張是期間，三張是萬元。

期間在上方 caption 已經寫過，但那行離卡片有兩段文字的距離，
而報酬率是**唯一一個脫離期間就無意義**的數字——+21.4% 是八個月還是三年，
差別極大。重複一次是為了讓卡片自己站得住。

`test_money_cards_carry_the_wan_reading` 原本斷言 metric[0] 沒有第二行，
改為斷言它等於期間字串。測試數維持 81。

### 2026-09-12（第二十二次）—— prompt 改為六段框架；規則壓縮會讓舊錯誤回來

使用者提了一版重寫的指示。框架確實比原本好，**但壓縮規則時把反例刪掉了** ——
而 WORKLOG 裡每一條防呆規則的反例，都是當初實測抓到的模型原話。
最後採用合併版：框架用新的，規則保留原本的反例。

**改了什麼**：五段（配置結構／分散效果檢驗／風險特徵／現金流特性／權衡與方向）
改為六段，把最後一段拆成「主要結構問題」與「調整方向」。
問題段要求每個問題交代「是什麼／從哪個數字看出來／實質影響」，
方向段要求對應到具體問題並按嚴重程度排列。
原本那段只列「集中度、資產類別廣度、再平衡機制、現金流需求」四個字眼，
等於允許模型講通則；實測輸出也確實是通則。

**三個實測發現（模型 `zai-org/GLM-4.7:fastest`，完整 payload 含夏普＋Beta＋配息）：**

**1. 模型照抄它看到的標題層級，不管文字寫什麼。** 使用者版把六段寫成 `###`
（因為包在 `## 分析框架` 底下），同時文字仍寫「使用二級標題」——
輸出六段全是 `###`。改回 `##` 後輸出就是 `##`。
新增 `test_framework_headings_are_all_second_level` 鎖住。

**2. 「調整方向」變具體之後會寫出個股買賣動作。** 使用者版對退休1 寫出
「考慮減少或**賣出**『群益臺灣加權正2』」，合併版第一版寫出「**賣出 00685L**，
將資金分配給核心持股或現金」。**現行版因為模糊反而踩不到這條線。**
台灣投顧是特許業務（見第二次），這是刻意保留的禁止項，不能因為框架變銳利就磨掉。
加了一句要求方向一律以配置層面（比重區間、缺哪一種風險來源）描述、
不得寫成對特定標的的買賣減碼換股動作，再測就不再出現買賣動詞，
改成「縮減高波動標的（如 2454）的**配置區間**」。

**3. 簡體字漏在產業用語，不是金融用語。** 輸出出現「产业链」「供应链」「走势」。
原本的繁中規則舉的例子全是金融詞（投資組合／報酬／負相關），**涵蓋不到這一類**。
已補上產業鏈／供應鏈／走勢三個例子。
**這一版還沒實測驗證** —— 驗證時撞上 402（見下），要補測。

**長度量測**（上限 2400，全部 `finish=stop`，沒有截斷）：

| 配置 | prompt | 輸出 tokens | 耗時 |
|---|---|---|---|
| 退休1（payload 4594 B） | 現行五段 | 718 | 4.3s |
| 退休1 | 使用者版六段 | 1286 | 7.1s |
| 退休1 | 合併版六段 | 1441 | 7.5s |
| 衝刺（payload 4145 B） | 使用者版 | 1242 | 8.7s |
| 衝刺 | 合併版＋買賣護欄 | 1389 | 7.7s |

輸出約增為兩倍，延遲從 4.3 秒增為 7–9 秒。2400 上限仍有充裕餘裕
（第十二次的教訓是上限剛好卡在自然長度就會每次截斷，這次不會）。

**擔心錯了的一件事**：原本預期刪掉「有效持股檔數兩者相等＝比重層面最分散」
這句會讓第三次那個錯誤回來。用衝刺（近等權重，有效 3 檔＝實際 3 檔）實測，
**使用者版與合併版都答對了**，沒有重現。規則仍然補回去了（成本低），
但這裡記一筆：**那次的回歸沒有被這次測試重現，屬於預防而非已證實的缺陷。**

**402 的正確判讀方式又更新了一次。** 第十六次記「402 是暫時性的」，這次前半段成立
（連續 5 次 402 後直接回 200），但到後半段變成連續 8 次、超過 6 分鐘都不通，
失敗頻率整個 session 單調上升，最後 App 上也持續顯示額度用完。
**兩種情況的區分是看趨勢，不是看單次。**

**但耗盡不是這次測試造成的，算術對不上。** 這次總共只成功打了 5 次完整請求
（每次約 4,000 input／1,300 output tokens）。照本檔記錄的實測單價
（$0.00004–0.00015／次）是 $0.0005，就算照公開牌價往高估也約 $0.03，
**都不到每月 $0.10 的全部**。合理的推論是餘額在這次之前就已經很薄 ——
第十六次因為「後來又打通了」而判定「額度並沒有用完」，
那個判定應該只是碰上暫時性的恢復，不代表餘額還充足。
**判斷依據是 402 的 response body 原文，不是帳單頁的數字。**
帳單頁會落後：實測伺服器回
`You have depleted your monthly included credits.` 的同時，
`settings/billing` 仍顯示 `Inference Usage $0.09 / $0.10`（還剩 $0.01）。
金額是四捨五入到分的，而且更新有延遲，兩邊不一致時以伺服器為準。
`whoami-v2` 則完全不回傳額度或用量欄位，查不到餘額。

**也驗證推翻過一個假設**：曾懷疑 402 是按 `max_tokens` 預扣額度造成的
（因為 `max_tokens=1` 的探測回 200、緊接著的完整請求卻 402）。
固定同一句極短輸入、只改 `max_tokens`（100／600／1200／2400）實測，
**四種全部 402** —— 不是預扣，是真的耗盡；
先前探測會過只是因為餘額恰好在那兩次呼叫之間跨過門檻。

另外那 8 次 retry 是白費的：402 不計費，但真的耗盡時重試到天亮也不會通。
retry 只對前半段那種暫時性 402 有用，設個 2–3 次上限就夠。
另外踩到一個陷阱：用 `max_tokens=1` 的輕量請求探測「額度是否恢復」**不具代表性**，
探測回 200、緊接著的完整請求照樣 402。要重試就重試真正那個請求。

**測試**：新增 2 個鎖 prompt 的測試。`GUARDS` 清單把每個反例字串連同
「它當初擋的是哪個實測錯誤」一起寫進去，日後有人要刪就會看到理由。
做過變異驗證：拿使用者的壓縮版去跑，8 條護欄缺 6 條、6 個標題層級全錯。
**在此之前沒有任何測試在看 prompt 內容**，所以規則退化一直是靜默的。81 → 83 passed。

順手把 `about.md` 寫死的「輸出上限 1100 tokens」改成 2400（第十二次改了程式沒改文件）。

### 2026-09-12（第二十三次）—— 402 訊息把附贈額度寫成了價格

使用者讀到「升級 PRO（每月 $2 額度）」，問「是 NTD 60 嗎」——
**把附贈的額度當成了訂閱費。** 查官方定價頁確認：PRO 是訂閱 **$9／月**，
附「20× included inference credits」，免費額度 $0.10 的 20 倍正好是 $2。
本檔先前也寫成「PRO $2.00／月」，同樣是錯的，一併改掉。

訊息現在同時寫出兩個數字並標明哪個是哪個。
順帶一提，以本專案的用量（單次約 $0.00004–0.00015）**買 credits 比訂 PRO 划算**，
$2 額度夠跑數百到數千次，除非要像 2026-09-11 那樣連續密集調校。

**改訊息時撞紅了既有測試，而且它抓得對。** 第一版寫成
「免費額度（$0.10）已用完」，把 `test_depleted_credits_says_so_instead_of_suggesting_a_retry`
斷言的連續字串 `免費額度已用完` 切斷了。改成「已用完（每月 $0.10）」
就同時滿足可讀性與斷言 —— **這種情況該改的是訊息不是測試**，
因為那句斷言守的是第十五次修掉的真實缺陷（402 不可讀成暫時性失敗）。
同時在該測試補兩條斷言：訊息必須同時出現 $9 與 $2、且不得出現「稍後重試」，
把這次的混淆本身鎖住。測試數維持 83。

### 2026-09-12（第二十四次）—— 錢字號被當成 LaTeX，訊息中段變成數學字體

使用者截圖回報「統一字體大小」。**不是字體問題** —— Streamlit 的 markdown 會把
一對 `$` 之間的內容當成 LaTeX 數學式渲染，所以「$0.10 ⋯ $9」整段變成數學字體、
兩個錢字號都消失，只剩最後沒有配對的 `$2` 是正常的。

**是前一則改動引進的。** 原本訊息只有一個 `$`（「每月 $2 額度」），湊不成對就不會觸發；
第二十三次補上價格後變成三個，前兩個立刻配成一對。
**加一個錢字號可能不會怎樣，加第二個就會。**

修法刻意不把跳脫字元寫進訊息常數：那是渲染層的事，寫進常數的話
下次有人加新訊息又會中一次，而且測試斷言、複製出去的文字都會看到多餘的反斜線。
改成 `ui.plain()` 純函式擋在 `st.warning` 那一刻，`ai_panel` 的 `ReadableError`
分支全部走它。日後任何帶價格的訊息自動免疫。

`STATUS_REASONS` 其餘四則（401/403/429/5xx）目前沒有錢字號，但也一併受保護。

新增 `test_prices_survive_markdown_rendering`，斷言三個價格都被跳脫、
且訊息裡不存在沒跳脫的 `$`。做過變異驗證：把 `plain()` 換成原樣回傳，測試如預期失敗。
83 → 84 passed。

**順手**：第一版測試把 `'\$0.10'` 寫成非 raw string，Python 會噴
`SyntaxWarning: invalid escape sequence`，測試數對但警告從 2 個變 6 個。已改 raw string。

### 2026-09-12（第二十五次）—— AI 失敗時指出出路，而不只是講失敗原因

額度耗盡的訊息把原因講清楚了（第十五、二十三次），但講完就沒了。
**使用者手上其實握著一份完整的數字，只是少了解讀** —— 第十七次做的
「取得完整輸入」複製鈕就能把指示與數字整份帶到任何聊天模型，
但那顆按鈕在展開區裡，出錯的當下不會有人想到它。
失敗訊息下方現在固定附一則提醒，指向該按鈕，並附 Google 財經連結供直接查行情。

**兩條失敗路徑收斂成 `_failure()`。** 原本 `ReadableError` 與通用例外各自
`st.warning`，只加在其中一條遲早會不一致。

**按鈕標籤抽成 `DISCLOSURE_LABEL` / `PORTABLE_LABEL` 常數。**
提醒文字裡要寫出「展開上方「X」，按「Y」」，而 X、Y 是別處的 UI 字串 ——
**這是會漂移的引用**：有人改了按鈕文案，提醒就會叫使用者去找一顆不存在的按鈕，
而且不會有任何錯誤。改成同一個常數之後結構上不可能不一致。

**Google 財經只放單一入口連結，不做逐檔深連結。** 實測
`google.com/finance/quote/0050:TPE` 對上市有效，但 **00984B（上櫃債券 ETF）
`:TPE` 與 `:TPEX` 都查無此標的**。逐檔連結會在部分持股上斷掉，
而在一則「出錯了」的訊息裡再放一個壞連結，比不放更糟。

新增 `test_every_failure_points_at_the_way_out`：兩種失敗都必須同時
`st.warning` 與 `st.info(FALLBACK_NOTE)`，且提醒裡要含兩個標籤常數與 Google 財經網址。
做過變異驗證：拿掉 `st.info` 後測試如預期失敗。84 → 85 passed。

### 2026-09-12（第二十六次）—— 展開區裡的 JSON 預設收折

`st.json(payload)` 預設整棵樹展開，佔掉一整個畫面，
把下方「帶著走」的複製鈕推到看不見的地方 —— 而那顆鈕正是第二十五次
才剛在失敗訊息裡指名要使用者去按的。改為 `expanded=False`。

數字本身沒有藏起來，點一下就展開；差別只在**預設不佔版面**。
展開區的用途是「想查的時候查得到」，不是「每次都攤在眼前」。

### 2026-09-12（第二十七次）—— 走勢圖加上依當前比重算出的組合線

使用者要求。**沒有新寫第七份組合淨值計算** —— WORKLOG 先前盤點過淨值散落 6 處
（再平衡那則評估裡），再加一份只會讓它們更容易彼此漂移。
新增 `correlation.blend_paths()` 直接建在 `portfolio_stats()` 的加權淨值路徑上，
只負責換算成圖表要的兩種座標。

**三個實作上的判斷：**

**1. 回撤模式下畫的是組合自身的回撤，不是各檔回撤的加權平均。**
這是最容易寫錯的一步：各檔在不同日子見高點，加權平均會算出一個比實際更深、
而且**實際上從未發生過**的回撤。測試用一組刻意錯開高低點的資料鎖住
（各檔回撤平均為 -10.0%，組合實際只有 -1.0%）。

**2. 比重對不上圖上的標的時不畫，並說明原因。** `weights` 的 index 是完整選股清單，
但圖上只有抓到行情的那些。抓失敗時把剩下的重新歸一化，會畫出一個
**使用者沒有設定過的配置**，而且看不出來。其他分頁遇到同樣情況是 warning 後停止，
這裡採一致的立場。

**3. 顏色用白色、線寬 3，不進 12 色輪替。** 組合不是持股，跟持股搶同一組顏色會讀成第 13 檔。
`render_chart` 因此多一個 `emphasis` 參數，JS 改 `lineWidth: item.width ?? 2`。
~~已知的小瑕疵：調色盤第 9 格是 `#F5F7FA`（近白），選滿 9 檔以上時兩者顏色接近。~~
**這句話是錯的，見第二十八次** —— 色槽按目錄位置決定，與選幾檔無關，四檔就撞上了。

**測試**：`test_correlation.py` 新增 3 個（與各標的同基準、回撤不是加權平均、
組合不會超出成分區間），`test_app.py` 新增 1 個端對端（預設勾選、切回撤與價格基準後不爆、
可取消勾選）。85 → 89 passed。

**變異驗證的一個發現**：把 `blend_paths` 換成「加權平均回撤」的錯誤版本後，
`test_blend_is_drawn_on_the_same_basis_as_each_holding` **抓不到** ——
因為 100% 押一檔時，加權平均回撤剛好等於該檔自己的回撤，那個案例是退化的。
真正擋住這個錯誤的是 `test_blend_drawdown_is_not_the_average_of_the_drawdowns`。
**單一持股的等價性測試對這類錯誤沒有鑑別力**，需要刻意錯開高低點的多檔案例。

**沒有動的**：CSV 匯出與「報酬與風險」表格仍只含各標的。
表格的欄位（年化報酬、年化波動）在組合層級另有定義與前提，
混進這張以個別標的為單位的表會需要一堆註腳；組合的那些數字在「風險分析」已經有了。

### 2026-09-12（第二十八次）—— 個股線淡化、白色收回給組合，並更正上一則的錯誤判斷

使用者截圖回報兩件事：個股線該淡化以凸顯組合，以及**白色重複了**。

**更正第二十七次**：我在那則把撞色寫成「已知的小瑕疵⋯選滿 9 檔以上時」。
**那個判斷是錯的。** 色槽是 `style_ids[s] % 12`，按標的在目錄裡的位置決定，
**跟選了幾檔無關** —— 00984B 就直接拿到第 9 格的 `#F5F7FA`，四檔照撞。
以「要很多檔才會遇到」當作可接受的理由，本身就建立在沒驗證過的推論上。

**修法一：近白色退出個股色盤**，第 9 格改為 `#7C5CFF`。白色從此專屬組合線。
**色盤與 `BLEND_COLOR` 一起搬到 `lightweight_chart.py`** —— 它們原本是
`streamlit_app.py` 裡的區域變數，測試 import 不到，這正是當初沒被守住的原因。

**修法二：組合線在場時，個股線淡化為 `rgba(...,0.42)`。**
組合不在場時不淡化 —— 那時沒有主從關係，淡化只是把整張圖調暗。
**圖例維持原色**：淡的是畫布上的線，不是身分識別；圖例的色塊與數值仍是全彩，
否則變成為了凸顯組合而犧牲辨識。

**測試用距離而非相等**：`#F5F7FA` 從來就不等於 `#FFFFFF`，它只是看起來像。
`test_no_holding_colour_can_be_mistaken_for_the_blend` 要求每個個股色與組合色的
RGB 距離大於 90（新色盤最近的是 `#D0A2FF`，104）。
變異驗證：把 `#F5F7FA` 放回第 9 格，測試如預期失敗並指名該色。

新增 `app/tests/test_chart.py`（3 個）。89 → 92 passed。

### 2026-09-12（第二十九次）—— 新增「配置比較」分頁

使用者要求：比較各種配置策略的走勢，含累積漲跌幅與歷史回撤。
新增頂層分頁（第 6 個，排在「行情比較」之後）與 `app/strategies.py`。

**幾乎沒有新數學。** 把每個配置的淨值當成一檔「價格」餵進既有的 `compare_prices()` ——
累積漲跌幅、回撤、年化報酬、年化波動於是**自動與行情比較頁同定義**，
不必再寫一份也不會日後各自漂移。淨值本身取自 `portfolio_stats()`（同第二十七次）。
真實行情交叉驗證：退休1 的共同期間算出 2026-01-23 — 2026-09-09、152 天，
與第十一次手動實測的數字完全一致。

**核心判斷：所有配置共用同一段期間。** 不同配置持有不同標的、上市日不同，
各自用「自己能涵蓋的最長期間」比較是沒有意義的 —— 持有較年輕基金的配置
期間較短，波動與回撤自然較小，會平白贏。取被選配置所有持股的交集。
`test_every_configuration_is_measured_over_the_same_window` 用
「只持有長歷史標的的配置也必須被截短」鎖住；變異驗證（改成各自用最長期間）如預期失敗。

**卡住起點的那一檔要指名，連同「誰持有它」。** 延續第十九次的作法，但這裡多一層：
知道是 009816 沒用，要知道退休1／3／4 都含有它，才知道移除哪些配置能換回更長的歷史。

**抓取迴圈抽成 `market.load_frame()`。** 這一頁需要的是預設配置的標的，
不是使用者當前選的，所以得自己抓。直接複製主頁那段 15 行的平行抓取迴圈會是第二份，
抽出來兩邊共用；主頁改用它之後既有 17 個 AppTest 全過，等於順帶驗證了重構。

**測試抓到一個真的 bug**：`render_chart` 把元件 key 寫死成 `performance_chart`，
兩個分頁各畫一張圖就 `StreamlitDuplicateElementKey`。已改為參數。
**如果沒寫那個端對端測試，這個會直接進到雲端** —— 純函式測試看不到元件註冊。

**兩個關於既有測試的教訓：**

1. **不該改 `st.tabs` 的 key。** 我一開始比照第十一次把 key 改成 `analysis_tabs_v2`，
   打壞 4 個用 `session_state['analysis_tabs']` 選分頁的測試。
   但第十一次要改 key 是因為**選項被移除**；這次只是新增一個標籤，
   `st.tabs` 記的是標籤不是索引，舊標籤全都還在，記憶仍然有效。**先例要看適用條件。**
2. **`labels[2:5]` 這種位置切片又壞了一次。** `test_risk_tabs_group_and_default_to_correlation`
   靠索引取子分頁，上方插入一個分頁就失效 —— 與第五次 `pick()` 誕生的原因同一類。
   改成錨定在父分頁：`labels[parent+1:parent+4]`。

**沒有動的**：配置的比重固定為 `PRESETS` 的設定值，這一頁不提供編輯；
「目前配置」取自側邊欄，隨使用者調整而變。

新增 `app/tests/test_strategies.py`（5 個）與 1 個端對端。92 → 98 passed。

### 2026-09-12（第三十次）—— 雲端在部署當下掛掉：熱重載留著舊模組

推上第二十九次之後雲端整個 App 壞掉，traceback 停在
`app/streamlit_app.py` 第 10 行的 `from market import ... load_frame ...`。
**推上去的內容是對的**（`origin/main` 的 `market.py` 確實有 `load_frame`、本機冷啟動正常、
98 測試全過），壞的是部署方式。

**成因就是環境陷阱第 5 條，只是這次發生在雲端。** Streamlit 在程式碼變更時
只重新執行入口腳本，`sys.modules` 裡的子模組留著不動。
於是新的主腳本去 import 舊 `market` 還沒有的 `load_frame` → 每次 rerun 都 ImportError，
要等有人手動 Reboot 才會好。**從外面看，就是「一推上去 App 就死了」。**
`module_compat` 的 docstring 早就寫明這個情境（"after a running deployment gains new
arguments"），但它只涵蓋 renderer 的參數；**我新增頂層名稱時沒有想到同一個機制適用。**

**修正不能放進 `module_compat`。** 那個模組自己也是舊的，
`from module_compat import 新函式` 會在同一個 rerun 以同樣方式失敗。
只能內嵌在每次都會重新執行的入口腳本裡，而且要在任何 `from ... import` 之前。

現在 `app/streamlit_app.py` 開頭有一份 (模組, 這版新增的名字) 清單，
缺了就 `importlib.reload`。穩定之後每次 rerun 只是 5 次 `hasattr`，成本可忽略。
**在既有模組新增頂層名稱、又要在入口腳本用到時，就往那份清單加一筆。**

**測試**：`test_a_running_deployment_heals_a_module_it_has_outgrown`
刪掉 `correlation.blend_paths` 模擬「比這版更早啟動的行程」，跑 AppTest 要能正常。
變異驗證把護欄關掉後，錯誤訊息是
`ImportError: cannot import name 'blend_paths' from 'correlation'` ——
**與雲端當下的失敗同一形態**，等於同時驗證了診斷與修法。98 → 99 passed。

**教訓**：本機冷啟動全綠、測試全綠，都證明不了熱重載部署會活。
這條路徑只有「舊行程 + 新腳本」才會走到，而那個組合在開發時不會自然出現 ——
要嘛刻意模擬，要嘛等它在雲端炸給你看。

### 2026-09-12（第三十一次）—— 第三十次的護欄修錯了：哨兵法擋不到「多一個參數」

第三十次推上去之後雲端**換一種方式壞掉**：不再是 ImportError，改成
`TypeError`，停在 `render_chart(..., key='strategy_chart')`。

**護欄的哨兵選錯了。** 我用 `hasattr(lightweight_chart, "COLORS")` 判斷模組新舊，
但 `COLORS` 是前一個 commit（`40e780c`）加的，`key=` 參數是下一個 commit（`87e415b`）加的。
雲端那個行程**有 `COLORS`、沒有 `key=`** → `hasattr` 通過 → 不重載 → 呼叫時 TypeError。

**真正的問題不是哨兵挑錯，是哨兵法本身。** 它要求每次改動都有人記得更新名單，
而「忘記更新的那一版」正好就是會壞掉的那一版 —— 這個設計把正確性押在人的記性上。
`module_compat.load_renderer` 用 `inspect.signature` 檢查參數，涵蓋面比 `hasattr` 廣，
但一樣要逐個指定要檢查什麼。

**改成不指定任何東西。** Python 在 import 時會把原始碼的時間戳寫進 `.pyc` 標頭；
git pull 換掉原始碼之後，那個戳記就跟磁碟上的檔案對不上。
凡是對不上的模組就 reload，而 reload 會重寫戳記，所以**會自己收斂**。
沒被換過的模組完全不碰 —— 這點很重要，見下。

**走過的彎路**：中間版本改成「第一次執行就把所有第一方模組全部重載」，
結果打爛 `test_app_controls_and_empty_state` ——
**把測試 patch 掉的 `market.load_symbol` 一併還原了**，變成真的去打網路。
`test_a_module_nobody_replaced_is_left_alone` 就是為了鎖住這件事：
無差別重載會撤銷行程在那些模組上做過的任何設定。

**踩到的精度問題**：`.pyc` 的戳記是**秒**為單位。兩個測試在同一秒內各自
`os.utime(path, None)` 會看起來像沒動過，所以測試改成把 mtime 明確推開 10 秒。
生產環境不會遇到（git pull 距離 import 遠不止一秒），但寫測試時會。

**測試**：三個 —— 少了名字要能救回、少了參數要能救回、沒被換過的模組不可被碰。
變異驗證把護欄關掉後，錯誤是
`TypeError: <lambda>() takes 1 positional argument but 2 were given`，
**與雲端當下的失敗同一形態**。99 → 101 passed。

**教訓**：第三十次我寫「以後同類改動會自癒」，但只驗證了 ImportError 那一種形態，
沒驗證「函式簽名改變」。**修好一種失敗形態不等於修好那個類別** ——
當時如果多想一步「除了少名字，還可能怎麼不相容」，就會發現哨兵法不夠。

### 2026-09-12（第三十二次）—— 配置比較加上各配置的內容

使用者要求圖下方顯示各配置的詳情。做成**矩陣**（持股為列、配置為欄）
而不是逐一條列：這一頁的用途是比較，矩陣讓「退休2 把 009816 換成 0050」
變成一列讀過去就看到，條列則要同時記住兩份清單。列依總比重由大到小排序。

**空白與 0.0% 必須不同。** 沒有這一檔留空白；`0.0%` 是真的指令 ——
列入比較清單但不投入（側邊欄允許輸入 0.0）。兩者長得一樣就會誤讀，
`test_a_holding_a_configuration_does_not_own_stays_blank` 鎖住這件事。

**顯示精度的小陷阱**：衝刺是 100/3，三欄都顯示 33.3%，逐欄相加是 99.9% ——
看起來像算錯。側邊欄另外把第一檔調成 33.4 湊整，但那是 `apply_preset` 的行為，
`PRESETS` 本身存的是 100/3。與其在這裡複製那套湊整邏輯，不如在說明裡講清楚
（實際計算用未四捨五入的值）——同 `年化報酬` 留空的處理原則：解釋，不要動數字。

新增 3 個測試（每欄仍須合計 100%、空白與 0.0% 要能分辨、大部位排前面）。
101 → 104 passed。

### 2026-09-12（第三十二次修訂）—— 改成各配置獨立卡片，並列三個

使用者要求改成「各配置獨立呈現、並列一列」，格式是標的名稱一行、比重靠右。
矩陣版（見下）因此撤掉，改為每個配置一張卡片。

**但六個並列一列在這個 App 裡不可行，是量出來的。** 主內容區扣掉側邊欄實測約 620px，
六欄每欄剩約 85px，標的名稱被逐字斷行（「元／大／台／灣」各佔一行），完全不能讀。
用真實寬度渲染兩個版本對照後改為**一列三個、折行**；三欄每欄約 190px，
只有最長的「00984B 大華優利美A債15」會折一次。
**若堅持一列，代價是只能顯示代號、拿掉中文名。**

`PER_ROW = 3` 抽成常數並在呼叫處寫明理由，否則下一個人只會看到一個沒來由的 3。

**順序改為「圖 → 各配置的內容 → 報酬與風險」**（使用者要求對調）。
先看清楚每個配置是什麼，再看它做出什麼結果；
反過來會先給一堆數字，而讀者還不知道那些名字各自代表什麼組合。

**比重格式**：`50%` 而非 `50.0%`，但三分之一仍保留 `33.3%` ——
`percent()` 去掉尾巴的 `.0`。使用者給的範例就是整數百分比。

**0% 的持股仍然列出**：它是「列入比較但不投入」的決定，
從卡片上消失會看起來像從沒考慮過這一檔。

**測試**：`percent()` 三種形態、由大到小排序、0% 要列出、每個預設配置仍須合計 100%
（手打的比重表少一位數不會報錯，只會安靜地少投資）。端對端斷言每張卡片都出現。
104 → 105 passed。

**踩到的測試坑**：我在 `.run()` 之後沿用舊的 AppTest 物件取 widget 值，
噴出 `KeyError: st.session_state has no key ...`。**`.run()` 回傳的是新的 app**，
舊物件的元件已經解析不到新狀態。斷言要嘛移到互動之前，要嘛接住回傳值。

### 2026-09-13（第三十三次）—— 「已自動縮短」在週末誤報；測試撞到日期才露餡
`session: claude-d1 [e01f5e]`

`test_period_banner_shows_once_and_flags_a_shortened_window` 今天（週日）紅燈，
但昨天全綠。不是測試脆弱，是**它今天才第一次撞到那個 bug**：

```
2026-09-12 → 起始 2025-09-12（五）→ 正常
2026-09-13 → 起始 2025-09-13（六）→ 誤報
```

預設期間是 today − 1 年。判斷縮短的條件原本是 `first.date() > start`，
拿**使用者選的日曆日期**去比第一個交易日——只要那天是週六日或國定假日，
第一個交易日必然比較晚，橫幅就會說「你選擇 …起，但**部分標的資料較晚**，已自動縮短」。

沒有標的資料較晚，是那天休市。**每個週末、每個連假都會發生**，
而且雲端當下正對使用者顯示這句錯話。前一天寫「104 passed / 0 failed」的
session 看不到，因為那天是週五。

改成 `first > covered.index[0]`——**有沒有標的真的被切掉**，
那正是這句話自己的措辭。順手把原本算兩次的 `prices.dropna(how="all")`
收成一個 `covered`，橫幅與下方理由區塊共用同一個判斷，不會再各說各話。

**既有測試擋不住這個**：它跑在真實的 today 上，週五執行一樣是綠的。
新增 `test_a_closed_market_on_the_start_date_is_not_a_shortened_window`，
讓所有標的都從同一個較晚的日子開始（模擬起始日休市），與執行日期無關。
把條件改回舊版實測會讓兩個測試同時紅，確認擋得住。測試數 105 → 106。

**教訓：與日期相關的判斷，測試要自己造日期，不要依賴執行當天。**

### 2026-09-13（第三十四次）—— 量化四個退休配置：報酬分不出來，風險分得出來
`session: claude-d1 [e01f5e]`

使用者問「哪個退休配置成功率最高」。**沒有改任何程式碼**，這則記的是量到的數據，
免得下一個 agent 重跑一次、或更糟——真的去算一個退休成功率出來。

**成功率算不出來，卡在資料長度：**

| 標的 | 起始 | 長度 |
|---|---|---|
| `0050.TW` | 2009-01 | 17.7 年 |
| `00662.TW` | 2016-06 | 10.3 年 |
| `00685L.TW` | 2017-03 | 9.5 年 |
| `00984B.TWO` | 2025-05 | 1.35 年 |
| `009816.TW` | **2026-01** | **0.63 年** |

後兩檔佔每個退休配置的 60–70%，共同期間只有 **153 個日報酬**。
退休成功率指的是「按某提領率提領 20–30 年、資金不歸零的機率」，
用 7.6 個月推 30 年會產出一個看起來專業、實際是雜訊的百分比。

**實測（2026-01-23 → 2026-09-11，還原價格，買進持有不再平衡，rf=2%）：**

| 配置 | 期間報酬 | 年化波動 | 最大回撤 | 夏普 |
|---|---|---|---|---|
| 退休1 | +20.1% | 20.9% | −12.1% | 1.58 |
| 退休2 | +18.4% | 20.7% | −11.6% | 1.44 |
| 退休3 | +22.1% | 22.0% | −12.4% | 1.66 |
| 退休4 | +22.3% | 25.2% | −14.8% | 1.47 |

**這張表的排名不能用。** 年化報酬的 95% 信賴區間寬 **105–127 個百分點**
（例如退休1：−20.1% 到 +84.9%）。改用配對差異（逐日相減，消掉共同部位）
區間縮到 9–23 pp，但仍然全部 p > 0.19：

| 比較 | 年化差 | 95% CI | p |
|---|---|---|---|
| 退休2 − 退休1 | −2.5% | [−7.0, +2.0] | 0.282 |
| 退休3 − 退休1 | +2.9% | [−1.5, +7.2] | 0.195 |
| 退休4 − 退休1 | +4.0% | [−7.6, +15.6] | 0.498 |

四者日報酬相關係數 **0.995–0.998**——它們不是四個策略，是同一個策略的四種四捨五入。

**但風險分得出來。** 變異數的估計收斂遠快於平均數，這是短樣本的基本不對稱。
整日重抽 bootstrap（4000 次，保留跨配置相關性）：

| 比較 | 波動差 | 95% CI | 可區分？ |
|---|---|---|---|
| 退休2 − 退休1 | −0.1 pp | [−0.5, +0.2] | ❌ |
| 退休3 − 退休1 | +1.2 pp | [+0.9, +1.4] | ✅ |
| 退休4 − 退休1 | +4.3 pp | [+3.6, +5.1] | ✅ |

**可下的結論只有一個方向**：退休4（00685L 槓桿 20%）承擔了**可測量**的額外風險，
換來**測量不到**的報酬優勢。退休2 與退休1 在風險上無法區分。

**若要真的算成功率，路線是用代理標的拉長歷史**（尚未做，需要使用者決定）：
`009816` 用 `0050` 代理（同為台灣 50 類），`00984B` 需要找長天期投等債 ETF 代理，
瓶頸會變成 `00685L` 的 9.5 年。9.5 年含 2018 修正、2020 疫情崩跌、2022 空頭，
壓力樣本尚可，但仍是單一循環且缺 2008。**代理是換標的，結論會帶入這個假設。**

### 2026-09-13（第三十五次）—— 退休配置評分：三種方法都說四個配置分不出來
`session: claude-ff [3b52fc]`

使用者要「客觀量化各配置的退休成功率並給評分」，提出四個面向：現金流穩定度、
質押需求、資產成長性、配置回撤。**沒有改程式**，這則記的是評估結論。

**成功率不要做。** 退休成功率指「按某提領率撐 20–30 年不歸零的機率」，
需要可信的長期報酬分布；而 009816 只有 0.63 年、00984B 1.35 年（合計佔配置 60–70%）。
產出的百分比會是雜訊。改用**所需報酬率**（門檻，純算術）與**敏感度網格**。

**實測數字（3000 萬、年開銷 150 萬、通膨 2%、質押利率 3%、50→90 歲）：**

配息 run-rate 要自己算，不能用近12月殖利率——00984B 2026-01 才開始配，
近12月窗口只含 9 次，5.72% 低估，實際 run-rate 6.27%。009816 上市至今**一次都沒配**。

| 配置 | 配息覆蓋 | 首年缺口 | LTV<50% 所需報酬 |
|---|---|---|---|
| 退休1 | 63% | 56 萬 | 5.89% |
| 退休2 | 69% | 47 萬 | 5.91% |
| 退休3 | 50% | 75 萬 | 5.86% |
| 退休4 | 63% | 56 萬 | 5.89% |

**代理歷史（用於路徑模擬，全部驗證過才用）：**
FX `TWD=X` 有 4 筆孤立壞值（跌到 1.8 再彈回），限制在 20–40 後乾淨，起點 2004-03 →
合成歷史上限 22.5 年，含 2008。**美元資產必須延後一天**（美股收盤在台股之後），
00662 的相關係數因此從 0.12 跳到 0.74。

| 腿 | 代理 | 真實年化／代理 | 相關 |
|---|---|---|---|
| 009816・0050 | ^TWII + 3.5% 股息 | 18.13% ／ 17.73% | 0.83 |
| 00662 | ^IXIC + 0.8%，延後1日，換台幣 | 18.90% ／ 18.60% | 0.74 |
| 00685L | 每日 2×(TWII+股息) − 1.5% | 42.39% ／ 40.84% | **0.96** |
| 00984B | LQD 換台幣，延後1日 | 6.38% ／ 6.43% | 0.47（最弱） |

**結論一：報酬假設的影響比配置選擇大一個數量級。**
區塊自助法（40 年、12 個月區塊、2000 條路徑、四個配置**共用同一組路徑**）：
台股腿用樣本值（11.4%）時斷頭機率 3.1–4.2%，降到長期 9% 時變成 16.2–18.9%。
橫向四個配置差 1pp 以內（分不出來），縱向換個假設就差四到五倍。

**結論二：連完全不含預測的結構指標也分不出來。**
所需報酬 5.86–5.91%、外幣資產全部 70%、實效槓桿 1.10/1.10/1.10/1.20、
有效持股檔數 2.9/2.9/3.3/2.9。這是第三次獨立確認（第三十四次用統計、自助法用路徑、
結構指標不用預測），**四個配置不是四個策略**。

**結論三：客觀量測挖出三件比排名重要的事。**

1. **配息 100% 來自單一標的**（退休1/3/4 全部來自 00984B，退休2 佔 91%），
   而它開辦半年內月配就從 0.126–0.145 砍到 0.081（−38%）。單點故障已經動搖過一次。
2. **外幣資產 70%**，開銷卻是台幣。四個配置都一樣，換配置解決不了。
3. **零成長情境下第 20 年全部斷頭**（維持率跌破門檻，不需要任何下跌）。

**所以評分該評「計畫」不是「配置」**：提領率 5.00%、所需報酬 5.9%、配息覆蓋 50–69%、
缺口 47–75 萬，加上情境式的脆弱性（「若下跌 X%，維持率變成 Y」）與結構曝險。
配置比較那一欄要誠實寫「在此數據下無法區分」。
「客觀」指的是不內嵌對未來報酬的預測，不是沒有選擇——衝擊幅度、質押利率、
維持率門檻都是參數，但使用者可以自己改、也知道自己在改什麼。

**還沒做**：稅（股利所得、二代健保補充保費；上述全為稅前，1500 萬部位的月配每次都會觸發門檻）、
其他收入（勞保年金會把缺口砍掉三到五成）、退休分頁本身的輸入與版面。

### 2026-09-13（第三十六次）—— Yahoo 少記了兩次 ETF 分割，1年區間就有一支是假的
`session: claude-ff [3b52fc]`

0052 富邦科技在 2025-11-17 做了 1:7 分割，0050 在 2025 年做了 1:4。
**Yahoo 兩次都沒有寫進 `Stock Splits` 欄位，而且只把調整套用到一部分序列**：
0052 從 2025-11-17 起是新單位、之前仍是舊單位；0050 的分界在 2014-01-02。
一條序列因此帶著兩種計價單位，中間那一階被當成暴跌。

**這不是 APP 算錯，是上游資料就是錯的，而 APP 沒有任何偵測。** 實測使用者看到的：

| | 修正前 | 修正後 |
|---|---|---|
| 0052・1年 | −70.9% ／ 回撤 −86.6% ／ 波動 93.1% | **+103.9% ／ −16.8% ／ 30.5%** |
| 0050・自訂 2010 起 | +208% ／ −77.3% ／ 26.5% | **+1132% ／ −33.8% ／ 18.8%** |

0052 的 1年、3年、5年全中（只有 3月／6月 乾淨）；0050 只有回溯到 2014 之前的自訂區間會中。
**五個預設配置用預設區間看，數字一個都不會變**——0052 不在任何預設配置裡。

**不寫死代號**，理由同第三十一次：下一支壞掉的一定是沒人記得加進名單的那支。
改成三個訊號同時成立才判定，門檻是量出來的，不是猜的：

| 訊號 | 門檻 | 實測 |
|---|---|---|
| 單日跌幅 | > 35% | 全樣本最慘的真實單日是 00685L 的 **−20.0%**（2倍槓桿，當天 TWII −9.7%） |
| 量能比（後20日中位÷前20日） | > 2.0x | 分割 6.37x／12.68x；真實崩盤 0.54–1.33x |
| 1/價格比離整數 | < 1% | 0050 是 4.0102（0.26%）、0052 是 6.9859（0.20%） |

**三者之間沒有重疊區**，任兩個就夠，要求三個是留安全邊際。
台股有 10% 漲跌幅限制，−20% 以上只可能是槓桿型或國外成分 ETF。
Yahoo 若正確記錄了分割，它同時會調整 Close、根本不留跳空，所以不必檢查 `Stock Splits`，
`actions=False` 不用改。

**配息的斷點跟價格不同天，這是查了才發現的。**
0050 的價格 2014-01-02 換單位，配息要到 2015 才換，中間夾著 2014-10-24 那一次
（配息 1.550 舊單位 ÷ 收盤 16.26 新單位 = 9.53%，實際應為 2.38%）。
**修好價格不會順便修好配息**，所以 `investment.repair_distribution_units` 是獨立的一套：
逐筆看「配息 ÷ 前一交易日收盤」，只有在**同一條序列已偵測到的分割比例**能把它拉回合理帶時才換算。
沒偵測到分割的序列一律不動，真正的大額配息因此安全。

**查過沒有權威來源，不要再查一次。** 證交所 OpenAPI 143 個端點全部看過，
沒有 ETF 受益權單位分割的歷史資料：`change/TWTB8U` 是一般股票的面額變更（2025 年只有
材料-KY、康霈、國巨、可寧衛），`TWT85U`／`TWTAWU` 是當日快照，
`t187ap47_L` 有「發行單位數」但只有出表當日、無歷史。ETF 分割靠公告發布，沒有 feed。

**刻意不處理的**：2317 鴻海（2000-01-04 −47%、1994-05-24 +118%）與 3008 大立光
（2002-03-12 −42%）是**除權配股**，比值 1.887／1.722／0.460 都不是整數。
舊台股的配股比例本來就不是整數，用觀察到的比值硬接回去可以讓序列連續，
但那等於拿掉整數檢查——**而沒有整數檢查就分不出配股與真實崩盤**。
目前選擇是放過它們（那幾支在很舊的自訂區間仍然失真），
若要處理，建議做成「偵測到但不校正，只警告」，不要直接改數字。

**測試 7 個**，其中最重要的是反向測試 `test_a_real_crash_is_never_rescaled`：
三種形態（深跌但未達門檻、跌幅與比例都像分割但股數沒變、量能跳升但比例非整數）
都必須原封不動。**誤判會抹掉使用者打開頁面就是要看的那次暴跌**，比漏判嚴重。
另有端對端測試確認畫面會講出來——校正後的數字跟原始來源對不上，沉默比 bug 本身更糟。
106 → 113 passed。

### 2026-09-13（第三十七次）—— 分割偵測改成分級：量能不再是門檻，還原價格才是對的搜尋對象
`session: claude-ff [3b52fc]`

第三十六次是「三項全部成立才校正」。掃完全目錄之後檢討，**那個設計的漏判來源就是量能那一項**：
冷門 ETF 分割時未必有 2 倍量能跳升，整條會被放過。三項改成分級。

**量能降級為描述，不再是門檻。** 真正有判別力的是另外兩項：
跳動幅度超過市場結構可能的範圍（台股 10% 漲跌幅限制；實測 118,589 個交易日裡
最慘的真實單日是 −20.0%），加上比例落在整數上。
**最小的整數分割 1:2 就是 −50%**，本來就遠在 −35% 門檻之上——所以門檻不會漏掉任何整數分割，
它只負責擋雜訊。量能仍然計算並顯示（0050 是 6.37x、0052 是 12.68x），但不參與判定。

**非整數比例改成「只警告、不校正」。** 配股的比例本來就不是整數，而**比例無法從序列本身還原**，
硬用觀察到的比值接回去等於拿掉整數檢查——那就分不出配股與真實崩盤了。
所以那類日期會被指名、說明「未做任何校正，涵蓋該日期的區間報酬與回撤可能失真」。
這讓第三十六次刻意放過的 2317、3008 從「靜默錯誤」變成「明講的已知限制」。

**補上反向分割**（合併，價格往上跳）。統一用 `divisor`：大於 1 是分割、小於 1 是反向分割，
`attrs` 的二元組形狀不變，所以 `investment.repair_distribution_units` 不用跟著改。

**搜尋對象從 `Close` 改成 `Adj Close`，這一項消掉了整整一類假警報。**
大額除息在 `Close` 上的跳動跟分割一樣大——2603 長榮 2023-06-30 是 **−39.7%**——
但那不是資料錯誤，是真實除息。同一天在還原價格上是 **+10.0%**，
而四個真正的股權變動（0050、0052、2317、3008）在還原後**跳動原封不動**。
還原價格因此零成本地把兩者分開。台股高息 ETF 很多，不做這件事的話真正的案例會被埋在除息日清單裡。

**掃描結果（34 檔、118,589 個交易日）**：校正 2 筆（0050 2014-01-02 1:4、0052 2025-11-17 1:7），
警告 3 筆（2317 兩筆、3008 一筆），**零誤判**。2603 不再被標記。

**證據強度要誠實看**：不誤判這面很強（零誤判，且 10% 漲跌幅限制是制度保證）；
抓得到那面**真陽性只有 n=2**，而且沒有權威來源可對照（證交所 143 個端點都查過），
所以「有沒有漏判」在原理上無法驗證。放寬量能是為了縮小這個未知，不是消除它。

113 → 117 passed。新增的測試裡兩個對應這次的改動：
`test_a_split_nobody_traded_is_still_repaired`（量能不跳也要修，就是漏判那條路）與
`test_a_distribution_the_adjusted_series_absorbed_is_not_reported`（除息不可被當成股權變動）。

### 2026-09-13（第三十八次）—— 為 00662 的分割預做演練，演練抓到偵測器會漏掉一半
`session: claude-ff [3b52fc]`

使用者說 00662 近期可能分割，要求先把 APP 準備好。**它在四個退休配置裡都佔 20–30%**，
跟 0052 不同——一分割就會直接影響使用者自己的配置。

**演練方式**：拿 00662 的真實序列，按 Yahoo 會產生的形態注入分割
（分割日之後全部 ÷N、之前不動），再跑完整管線對照「沒分割時的真實值」。

**演練立刻抓到 bug**：注入在 5 個交易日前那筆沒被偵測到，1、2、3、20 卻都過。
不是隨機，是**容差太緊**。

**根因**：分割當天的比值不是 N，是 **N ÷ (1 + 當天自然漲跌)**。
0050 與 0052 當天剛好只動了 −0.25% 與 +0.20%，所以 1% 容差過得去——**那是運氣**。
00662 平常一天就動 1.3%，1% 容差大約會漏掉一半的分割日。

**試過但放棄：改用開盤價。** 想法是台股分割後恢復買賣的參考價就是
「停止買賣前收盤價 ÷ 換股率」，用開盤可以消掉當天的漲跌。實測推翻：

| 標的 | 類型 | 收盤比／離整數 | 開盤比／離整數 |
|---|---|---|---|
| 0050 | 分割 | 4.0102／0.26% | 4.0000／**0.00%** |
| 0052 | 分割 | 6.9859／0.20% | 6.9483／**0.74%**（更差） |
| 2317 | 配股 | 1.8870／5.65% | 1.9500／**2.50%**（更危險） |

開盤對 0050 有效、對 0052 反而更差，而且**把 2317 的配股推得更靠近 2**，
更容易被誤判成分割。這條路不能走。

**採用的解法：分段容差。**
比例貼得極近（≤1%）就自己成立；落在 1–4% 的模糊帶要求量能 ≥2x 佐證；超過 4% 一律只警告。
關鍵是 **2317 的 1.887 離 2 有 5.7%，落在 4% 之外**，所以拉寬到 4% 不會把配股拖進來；
再寬到 6% 就會。量能因此不是回到第三十六次的門檻角色，**只在模糊帶當佐證**。

**量出來的抓到率**（把分割注入 1 年區間內每一個可能日期，236 個位置 × 四種比例）：

| 情境 | 抓到率 | 修正正確性 |
|---|---|---|
| 量能隨股數上升（實測 6.4x／12.7x，真實情況） | **236/236 = 100%** | 全部正確 |
| 量能完全不變（病態假設） | 156/236 = 66.1% | 全部正確 |

**改之前，即使量能上升也只有 66%。** 零次錯誤修正。

**順帶量到的**：00662 是**國外成分 ETF，沒有 10% 漲跌幅限制**，所以第三十七次
「制度保證」那個論證對它較弱，只能靠實測餘裕。它上市 2499 個交易日以來
最大單日跌幅 −14.3%（2025-04-07）、最大漲幅 +12.3%，**離 35% 門檻還有 2.4 倍**。
超過 ±35% 的日子：0 天。

**教訓**：第三十七次收尾時我寫「整數比例的分割現在應該抓得到全部」。
那句話只在「分割當天價格不太動」的前提下成立，而我沒有檢驗那個前提。
**演練比推理可靠**——同一個教訓第三十次、第三十一次各出現過一次，形態都是
「修好一種形態就以為修好整個類別」。117 → 118 passed。

### 2026-09-13（第三十九次）—— 00662 確定 1 拆 5：停牌會讓比值對不上整數
`session: claude-ff [3b52fc]`

使用者提供富邦投信公告：**00662 確定 1 拆 5，11/11–11/16 停止交易，11/17 恢復並以新單位掛牌**。
（今天是 2026-09-13，所以是 2026-11-17。）按公告的形態做針對性演練，**12 個情境全部失敗**。

**根因：停牌期間基金不交易，但它追蹤的那斯達克 100 繼續動。**
11/17 恢復時的價格反映四個交易日的淨值變化，比值因此是 **4.7944 不是 5**，
離整數 4.11%，剛好落在第三十八次訂的 4% 模糊帶外面。
**0052 當初沒踩到，是因為它的停牌只跨過週末、沒有損失任何交易日**（11/14 五 → 11/17 一）。

**解法用的是市場機制上的差異：台股分割一定先停止買賣，除權配股不會。**
停牌在 Yahoo 資料裡是「沿用前一日收盤 + 成交量 0」（用 1218 泰山 2026-08-13 的停牌確認過，
不是 NaN）。所以「步階前面緊接著停牌日」本身就是分割的證據，可以據此把容差放寬到 10%，
而配股永遠拿不到這個豁免。實測 2317 的 2000-01-04 前面沒有停牌日，仍然只警告。

**順手修掉一個相關的洞**：量能中位數現在排除成交量為 0 的列。
停牌日不是「安靜的交易日」而是「不存在的交易日」，若使用者選的區間起點落在停牌附近，
四個 0 會把中位數拉掉、讓佐證失效。

**量到的容許範圍**（4 個停牌日、1:5）：

| 停牌期間漂移 | 結果 |
|---|---|
| −11% ~ +10% | 正確辨識為 1:5 ✅ |
| +11% 以上 | 退化為「只警告不校正」 |

00662 四個停牌日的漂移標準差約 2.6%，±10% 大約是 3.8 個標準差。

**測試逼出一個本質限制，已寫進註解**：漂移 25% 時比值 `5/1.25 = 4.0`
**正好落在另一個整數上**，會被當成 1:4 校正。
「1:5 漂移了」與「1:4 沒漂移」在序列裡留下完全相同的步階，**沒有任何訊號能分辨**。
危險區間是漂移 11–14%（約 4–5 個標準差）；超過 14% 會落在所有帶之外、退回只警告。
這不是停牌放寬造成的，是「從價格步階推論比例」這件事的本質限制。
容差停在 10% 就是為了卡在這個區間之前。

**演練結果**：修正後 12 個情境（3月／6月／1年／3年 × 恢復當日／三日後／年底）
全部與「沒分割時的真實值」完全相同。118 → 120 passed。

**教訓（第三次同型）**：第三十八次我用「注入在任意日期」演練，覆蓋了「當天有漲跌」，
但**沒有覆蓋「停牌好幾天」**——因為 0052 的案例裡沒有這件事，我就以為不存在。
演練的價值取決於演練的形態有多像真的，而**真實公告比我想像的形態多一個維度**。

### 2026-09-14（第四十次）—— 緩衝池策略評估：提領規則的影響比配置大一個數量級
`session: claude-ff [3b52fc]`

使用者拿一支 YouTube 影片的策略來問「是不是比我的配置更安全、穩定、優雅」。
**沒有改程式**，這則記的是量到的東西，以及一個把先前結論推翻的方法論錯誤。

**策略內容**（使用者轉述，我看不了影片）：3000 萬分 112 等分，100 份投 009826（成長池）、
12 份投 00865B（緩衝池）；每年由成長池撥 4% 進緩衝池，生活費 =（成長池×4% + 緩衝池）× 25%。

**那個公式是代數恆等式**：緩衝池 12 份 + 當年撥入 4 份 = 16 份，取 25% 正好 4 份 = 撥入量。
所以 `(G×4% + B) × 25% ≡ G × 4%`，緩衝池第一年進出打平。
**12 ÷ 4 = 3**，緩衝池正好是三年提領量——這就是 100:12 這個比例的來由，典型的三年緩衝設計。

**兩檔標的的資料長度**（影片的「30 年回測」不可能是回測這兩檔產品）：

| 標的 | 名稱 | 上市 | 長度 |
|---|---|---|---|
| 009826 | 貝萊德世界股票 | 2026-07-22 | **0.14 年（7 週）** |
| 00865B | 國泰US短期公債 | 2019-11-15 | 6.82 年，年化 2.89%、波動 5.1% |

**我犯的錯，以及它如何翻轉結論。**
我最初把它模擬成「固定實質金額提領」，量出「緩衝池機制幾乎沒有用」（破產機率 0.5% vs 0.5%）。
使用者提供正確公式後才發現**那是動態提領**——每年取當下組合的一個比例。
兩者是完全不同的東西：

- **動態提領依定義不可能破產**（永遠只拿走一部分），所以「破產機率」根本不是該問的指標
- 固定金額提領下緩衝池確實沒事做；**配上動態提領，它才是讓浮動收入變得可以生活的那個零件**

**改用正確規則後量到的緩衝池價值**（同樣 3.57% 提領率、同樣資產，只差有沒有緩衝池）：

| | 最差單年收入 | 跌超過 10% 的年份 | 期間最低（P5） |
|---|---|---|---|
| 有緩衝池 | **−12.9%** | 2.9% | 52.5 萬 |
| 無緩衝池 | −37.1% | 10.1% | 48.5 萬 |

**把最糟的單年收入衝擊從 −37% 壓到 −13%。** 這是很大的實質效果。

**這個策略真正的代價：它要的錢比較少。**
`3000 萬 × 3.571% = 107.1 萬／年`；要領到 150 萬需要 **4,200 萬**。
安全不是免費的——它安全是因為少要 29% 的錢。交換的形狀是
**「可能破產」換成「收入會波動」**，而後者可以調整、前者不行。

**代理驗證**（使用者提議用 VT）：

- VT vs ACWI 日報酬相關 **0.978**，年化 8.90% vs 8.76%、波動 20.5% vs 20.1%——可互換，換誰結論都不變
- 合成世界股票 `SPY 60% + EFA 30% + EEM 10%`：對 VT 相關 **0.975**、對 ACWI **0.989**，
  波動幾乎相同。固定權重比真品每年高約 0.8pp（權重不隨市值漂移、無費用），已扣除
- 合成序列起於 2003-04（受 EEM 限制），**可把窗口從 2008-03 前推到 2004-03**

**窗口起點很重要，2008-03 起算是錯的。** 那個起點落在金融海嘯半途，
錯過 2007-10 的高點與第一段下跌。改用 2004-03 之後世界股票的最大回撤是 **−48.0%**，
壞情境下的最低年收入從 83 萬掉到 52.5 萬——**先前那個數字是樣本沒涵蓋完整崩盤造成的**。

**真實歷史那一條路徑（2004-03 退休，第 5 年撞上 2008）**，今日幣值、萬元：

```
110 105 113 114 114 101  95 101  99 112 110 122 119 119 127 129 125 129
```

**收入最低 95 萬（第 7 年），回落 −17.0%，三年回復，期末資產 3,000 → 5,629 萬。**
自助法的尾部（P5 最低收入 52.5 萬）比真實歷史差很多，因為區塊重抽會把歷史上
沒有連續發生過的壞年份串在一起。兩個數字要一起看。

**尚未用上的機會**：`^990100-USD-STRD` 是 MSCI World 指數，**1972 年起、54.7 年**，
涵蓋 1973–74、1987、2000–02、2008 四次大空頭。
**目前所有分析的瓶頸都是匯率**（`TWD=X` 只到 2004-03）。台幣兌美元三十年在 25–35 之間、
實測年化 −0.06%，長期幾乎不貢獻報酬，所以**用美元基準跑長樣本是可接受的取捨**——
換來四次完整空頭而不是一次。要認真談順序風險，那個樣本才夠。

**對退休功能的設計結論（重要，會改變第三十五次的方向）：**

前面量了三次「四個配置分不出來」，這次量到**提領規則的影響大得多**——
同樣的資產，換規則可以把破產機率從 19% 變成 0%。
所以那個分頁要比較的**不是配置，是提領規則**：固定實質金額、固定比率、緩衝池、護欄法。
而且規則的差異是**機制性的，不需要預測未來就能量出來**，正好符合使用者最早要求的「客觀」。

**教訓**：我用錯誤的規則模擬了兩輪，量出來的東西內部一致、看起來很有說服力，
但回答的是另一個問題。**使用者更正公式之後結論直接翻轉。**
外部給的規格若算出不合理的結果（第一版算出年生活費 17 萬），
要回頭質疑自己的解讀，而不是接受它繼續算下去。

### 2026-09-14（第四十一次）—— 緩衝池法續談：自我平衡的代數、通膨壓力測試、一個稅務更正
`session: claude-ff [3b52fc]`

接續第四十次，**沒有改程式**。使用者逐點追問，逼出幾個第四十次沒寫到的東西。

**一、「它給你 107 萬」是錯的框架，我講錯了。**
動態提領的收入會成長：4% 撥出率下中位路徑 111 → 146（第10年）→ 195（第20年）→ 338 萬（第40年，今日幣值）。
但「會成長到超過需求」同樣不準——**中位路徑第 11 年才首次超過 150 萬**，
而且第 20 年仍有 33%、第 40 年仍有 18% 的機率低於 150 萬。
**真實歷史更保守：2004 退休、撥出 4%，十八年收入從 110 走到 129 萬，一次都沒到過 150 萬**，累計缺口 657 萬。

調高撥出率可以把分布上移但消不掉波動：

| 撥出率 | 首年 | 第20年 | 第40年 | 長期低於150萬機率 | 40年末資產 |
|---|---|---|---|---|---|
| 4.0% | 111萬 | 195萬 | 338萬 | 33%（第20年） | 20,878萬 |
| 5.6% | 149萬 | 199萬 | 247萬 | ~30%（一直） | 10,767萬 |
| 7.0% | 180萬 | 190萬 | **174萬**（倒退） | — | 6,016萬 |

**7% 是上限的訊號**：第 40 年收入低於第 20 年，代表提領開始吃本金。
**結論：任何按比例提領的規則，在原理上都給不了保證的固定數字。**
若使用者的 150 萬含硬性支出（房貸、長照），需要另外用年金或債券梯把那一塊鎖住。

**二、規則為什麼不需要再平衡（代數解）。**
設 B/G = x，每年 `B' = B + 0.04G`、支出 `0.25B'`、成長池剩 `0.96G`，均衡點為

```
x = 0.04k / (1 − k)，  k = 0.75(1+r_b) / (0.96(1+r_g))
```

代入實測報酬（世界股票 9.5%、美短債 2.8%）得 x ≈ 0.110，**緩衝池佔總資產約 9.9%**，
幾乎正好是起始的 10.7%。自助法驗證：第 1/10/20/40 年的中位數為 10.36/10.24/10.31/10.24%，
**四十年不漂移**。對照組（固定金額提領，第四十次模擬錯的那版）會漂到 34.8%。

**2008 的自動反應是這個設計最漂亮的地方**（緩衝池佔比逐年）：
`10.0 9.9 10.0 10.9 17.9 12.5 11.0 11.5 11.0 10.1` ——
崩盤時分母掉了、佔比自動跳到 17.9%（**在最該保守時自動保守**），
兩三年後自動回到 10–11%（**在最該加碼時自動加碼**），全程零額外交易。

**三、成長池換成台股（009816／0050）會更好嗎？數字說會，結構說不要。**

| 成長池 | 第20年收入 | 第40年收入 | 最差單年 | 40年末資產 |
|---|---|---|---|---|
| 世界股票 | 193萬 | 338萬 | −13.0% | 21,273萬 |
| 台灣TOP50 | **234萬** | **520萬** | −13.6% | **33,062萬** |

而且真實 2008 那段台股版**收入更穩**（最低 −8% vs 世界股票 −20%），因為台股跌得深但回得快。
**但回測看不到的是**：使用者的房子、現金、健保、物價全在台灣，
**再把 89% 退休資產押台股等於把人生押在同一個經濟體**。
關鍵是尾部方向——**台海若出事，台股與台幣同時重挫，而世界股票（台幣計價）會因台幣貶值而上漲**；
世界股票對沖的正是台股集中化製造的那個風險，**而那個情境在 2004–2026 樣本裡一次都沒發生**。
另外 0050 有超過一半集中在單一公司。建議不換；若要折衷，成長池可世界／台股各半。

**四、三個結構性優勢，量化如下。**

| 優勢 | 量化 |
|---|---|
| 利率風險 | 組合存續期間 **7.00 年 → 0.21 年**（33 倍） |
| 免再平衡 | 均衡點 ~10%，40 年不漂移 |
| 免質押 | 退休1 每年需借 56 萬；緩衝池法零借款 → 破產機率 16–19% → **0%** |

**2022 年是最好的實證**（股債同跌）：退休1 全年 **−16.6%**、緩衝池法 **−9.8%**。
**持有 89% 股票的組合，在 2022 年跌得比持有 50% 債券的少一半**，因為那年債券才是元凶。

**代價要記在帳上**：緩衝池法組合最大回撤 **−49.5%** vs 退休1 的 −34.3%，波動 19.4% vs 13.1%。
**不是比較不會賠，是比較不會毀滅**——虧損更深但不會演變成出局。

**五、通膨壓力測試（使用者指出的第四點，這是四點裡最強的一點）。**

| 通膨 | 緩衝池法實質收入（第40年） | 退休1 斷頭機率 |
|---|---|---|
| 2% | 339萬 | 5.1% |
| 3% | 337萬 | 8.5% |
| 4% | 334萬 | 12.8% |
| 5% | **334萬** | **19.0%** |

緩衝池法實質收入幾乎不受通膨影響（差 1.5%），退休1 斷頭機率變成**近四倍**。
退休1 被通膨三面夾攻：支出隨通膨漲、**票息名目固定完全不漲**、
升息再依存續期間 7.0 年造成一次性資本損失（升 3% 即 −21%）。
緩衝池法**不需要抗通膨設計**——提領是資產的一個比例，通膨推高名目資產就自動推高提領。

**限制要講明**：股票是**長期**抗通膨工具不是短期的（1970 年代美股實質報酬負了十年），
上面「完全轉嫁」的假設對它偏寬鬆。但兩者的差別在**損失能不能恢復**：
股票被壓低的購買力會隨盈餘重新定價回來，**債券票息被侵蝕的購買力永遠回不來**。

**帳要算對**：利率風險與抗通膨**是同一個曝險的兩面**（長天期名目債券），所以是三件獨立的事，不是四件。

**六、稅務更正——我講錯了。**
第四十次我寫「00984B 每月配息會觸發二代健保補充保費」，**這是錯的**。
**補充保費只課中華民國來源所得，而 00984B 投資美國公司債、配息屬海外所得，不在課徵範圍**。
海外所得走最低稅負制：一申報戶全年未達 100 萬**完全不計入**基本所得額，
計入後還要基本所得額超過 750 萬才課 20%。退休1 在 1500 萬部位上年配約 94 萬，**剛好在門檻下，實際稅負可能是零**。

真正會被課補充保費的只有台灣來源股利（退休2 的 0050，600 萬部位年配約 8.9 萬 → 補充保費約 1,900 元），金額可忽略。
**所以「靠資本利得生活比較省稅」這個方向性優勢不成立**，兩者實際稅負都接近零。
僅存的技術性差異：賣出台灣掛牌 ETF 是證券交易所得（停徵，且**不計入基本所得額**），
收海外配息要計入基本所得額。在目前金額下不構成選擇理由。

**教訓**：稅務結論不要從「常識」推導。我把海外債 ETF 最常被提到的**優點**寫成了缺點，
而使用者一句話就推翻。涉及稅制的判斷要麼查證、要麼明講不確定。

### 2026-09-14（第四十二次）—— 新增「退休5」：成長池 90% ＋ 緩衝池 10%
`session: claude-ff [3b52fc]`

把第四十、四十一次評估的緩衝池法做成預設配置：`009826 貝萊德世界股票 90%` ＋
`00865B 國泰US短期公債 10%`，3,000 萬。

**為什麼是 90/10 而不是影片的 100/112（89.3/10.7）**：實測四種起始比例
（100/112、90/10、85/15、95/5）在第 40 年**全部收斂到緩衝池佔比 10.24%**，
與代數解 9.92% 吻合——**均衡點只由撥出率與支付率決定，與起點無關**。
收入約五年內收斂（第5年 124.8 vs 123.9 萬，差 0.7%）。
90/10 首年少 5%（102 vs 107 萬），但好執行太多。

**踩過的陷阱要記**：85/15 首年給 142.5 萬、很接近 150 萬的需求，看起來像捷徑，
但第 5 年掉到 130.3 萬、第 20 年 185.8 萬還輸給 90/10 的 196.4 萬。
支付率固定 25%，**放大池子只是把水一次多放一點，接著被拉回均衡**，
結果是開高走低——退休現金流最糟的形狀。**要提高收入的旋鈕是撥出率，不是池子大小。**

**這是第一個所有持股都不在 `CATALOG` 的預設配置。**
名稱靠 `securities.json`（兩檔都有），選項靠 `apply_preset` 主動加進 `symbol_options`。
新增 `test_a_preset_whose_holdings_are_all_outside_the_catalogue_still_applies` 鎖住這件事——
少了 apply_preset 那一行，multiselect 會把整個配置丟掉。120 → 121 passed。

**已知副作用，暫不處理**：009826 於 2026-07-22 上市，只有 7 週資料。
「配置比較」預設全選，所以加入退休5 之後整頁的共同期間從 **154 天砍到 38 天**：

| 比較內容 | 共同期間 | 交易日 |
|---|---|---|
| 含退休5 | 2026-07-22 → 09-11 | 38 |
| 不含退休5 | 2026-01-23 → 09-11 | 154 |

頁面會正確指名原因（第二十九次的 `binding_holding`：「起點由 009826 決定，含有它的配置：退休5」），
所以**機制是對的，只是預設值不理想**。不改的理由：這是暫時狀態，009826 會長出歷史，
為暫時的資料狀況改預設行為不划算。要長期比較就在多選框拿掉退休5。

**別拿 38 天的數字下結論**：那個區間裡退休5 顯示波動 6.9%、回撤 −3.2%，看起來最穩，
但那是七週。真正的評估在第四十、四十一次，用的是 22.5 年的代理序列。

### 2026-09-14（第四十三次）—— 支援美元計價標的，並新增「退休6」當退休5 的長歷史替身
`session: claude-ff [3b52fc]`

使用者要求能選 VT。**原本不行也不該硬加**：`parse_symbols` 只收 4–6 位數字，
而就算寫進 PRESETS 繞過檢查，APP 沒有匯率層——會把 VT 的**美元報酬**
跟 00865B 的**台幣報酬**加權平均，算出一個兩種幣別都不是的東西。
實測一年就差 **5.3 個百分點**（VT 美元 +18.5%、台幣 +23.8%，台幣同期貶 4.4%）。

**做法**：`currency_of`（有 `.TW`/`.TWO` 或 `^TWII` 為台幣，其餘為美元）、
`fx_rates()`（快取的 `TWD=X`）、`to_twd()`，掛在 `load_symbol` 內部，所有分頁自動繼承。

**不延後一天，這點容易搞錯。** 我在分析腳本裡對美元序列用 `shift(1)`，
那是在比對「台灣掛牌基金 vs 美股指數」——台灣基金用前一日美股收盤定價。
而把美元資產換成台幣是**同一時點的估值**，同日相乘才對。兩件事不要混。

**順序是先修分割再換匯**：兩者都是乘法，而整數比例的檢查必須在乘上匯率之前做。
`test_a_split_is_repaired_before_the_currency_is_applied` 鎖住。

**缺匯率的日子丟掉，不補值。** `TWD=X` 只回溯到 2004-03，且有 4 筆孤立壞值
（2011-10-25/26、2014-12-31、2015-01-01，跌到約 1.8 再彈回），用 20–40 區間濾掉。
清理後 5,415 筆、最大單日 5.0%、零異常。**假造的匯率一旦進到報酬裡就跟真的沒兩樣**，
所以寧可讓那些日子消失，由既有的共同期間機制去解釋。

**驗證**：VT 換算後與手算的最大差異 `0.0000000000`；
`0050.TW`、`00865B.TW` 未被換算（雙重換算會讓整條序列乘上約 30 而下游毫無察覺，
`test_a_dollar_price_is_converted_and_a_taiwan_price_is_not` 的後半就是為此而寫）。

**仔細測試抓到一個這個功能才會暴露的既有缺陷。**
`correlation.analysis_data` 取的是**連續完整**區段。美股與台股假期不同，
每隔幾週就有「一邊開市一邊休市」的日子，那不是資料缺口而是行事曆差異，
但它會把連續段切碎——實測「退休1–4 + 退休6」的共同期間只剩 **45 天**（應為 151 天）。
主頁的 `compare_prices` 用交集（`dropna`）所以沒事；受影響的是相關性、夏普、Beta、
投資報酬、配置比較五處。
改成**只在缺口長到不像假期時才切斷**（`MAX_CLOSURE_DAYS = 15` 日曆天；
台股農曆年是最長的常態休市，約九天）。修好後回到 151 天。
非日期索引（既有測試用整數索引）走退化路徑：任何跳號都算切斷，維持原行為。

**parse_symbols 放行純字母代碼**，並更新既有測試——它原本斷言 `AAPL` 會被拒，
那是舊行為。新測試同時鎖住仍要被拒的形態：`ABCDEF`、`2330.US`、`12AB34`、`0050.TWX`、`00-50`。

**揭露**：畫面會講「已按當日匯率換算為新臺幣，報酬因此包含匯率變動…與美元帳戶對帳單
的數字會不同，差額就是台幣的升貶」。使用者拿券商帳單對不上時必須看得到原因。

**新增「退休6」= VT 90% + 00865B 10%**，目的是當**退休5 的長歷史替身**
（009826 只有 7 週）。刻意沿用同一檔緩衝池標的，這樣兩者只差成長池。

**但實測它的瓶頸不是 VT 而是 00865B**：

| 標的 | 起始 | 長度 |
|---|---|---|
| VT | 2008-06-26 | 18.2 年 |
| **00865B** | **2019-11-15** | **6.8 年** ← 退休6 的實際上限 |
| 009826 | 2026-07-22 | 0.1 年 |
| SHY（換算後） | 2004-03-24（受匯率限制） | 22.5 年 |

所以退休6 給的是 **6.8 年**（含 2020 疫情崩跌與 2022 空頭），不是 18 年。
要更長就得把緩衝池也換成美股標的（VT + SHY 可得 18.2 年），代價是不再沿用退休5 的實際持股。
**現在使用者可以自己在側邊欄輸入 SHY 做這件事**，不需要再開一個預設配置。

**退休5 與退休6 日對日不會一致，這是時差不是追蹤失準。**
實測 VT 延後 0/1/2 天的相關為 **0.303 / 0.590 / 0.275**——延後一天明顯最高，
因為 009826 用前一日美股收盤定價。長期報酬不受影響，時差只是把同一段報酬挪一天。

**行事曆交集的代價**：1 年區間下 VT 250 天、00865B 242 天，交集 237 天
（VT 損失 13 天、00865B 損失 5 天）。這是跨市場組合無法避免的，已由既有機制處理。

121 → 130 passed（新增 9 個）。

### 2026-09-14（第四十四次）—— 實機檢查：跨時區的 Beta 會系統性接近 0
`session: claude-ff [3b52fc]`

使用者要求「先在本機做並檢查所有功能」。把 `smoke_live.py` 從 4 檔標的的取數檢查
擴充成 **10 節、39 項**的完整整合檢查（真實資料、混合幣別、逐一渲染每個頁面）。
`python app/tests/smoke_live.py` 可重跑；會打網路，所以不在 CI 內。

**檢查抓到一個真實的解讀陷阱。** VT 對 0050 的 Beta 只有 **0.02、R² 0.00**，
看起來像「世界股票與台股完全無關」。實測延後一天：

| 標的 | 幣別 | 同日 Beta / R² | 延後1日 Beta / R² |
|---|---|---|---|
| VT | USD | 0.02 / 0.00 | **0.33 / 0.25** |
| SPY | USD | 0.01 / 0.00 | **0.34 / 0.24** |
| 00662.TW | TWD | **0.59 / 0.50** | −0.06 / 0.01 |
| 2330.TW | TWD | **1.16 / 0.85** | −0.01 / 0.00 |

**美股收盤在台股之後，同一天的報酬根本不重疊**，所以跨市場的同日回歸必然接近 0。
台灣掛牌標的則相反——同日有關聯、延後一天就沒了。

**不改算法，改講清楚。** 偷偷把資料延後一天會讓「Beta」變成另一個東西
（而且要決定延後哪一邊、反向怎麼辦）。改成在 Beta 分頁出現警語，
並在 AI 的 payload 加 `時區注意` 欄位，明講「不可據此說它們與市場無關或風險低」。
這與既有的「基準對自己回歸必得 1.00」那個提醒是同一類處理。

**測試過程本身踩到兩個 AppTest 的坑，記下來免得重踩：**

1. **元件註冊綁 runtime。** `lightweight_chart` 在 import 時把元件註冊進「當時的」runtime；
   若在建立 AppTest 之前已經 import 過（第 8 節的 `strategies` 會），
   新 runtime 就找不到它，八個頁面全部噴
   `Component 'taiwan_lightweight_chart' is not registered`。
   `test_app.py` 的 `new_app()` 早就處理過（`sys.modules.pop`），但那個知識沒寫在 WORKLOG 裡。
2. **AppTest 無法選取巢狀分頁。** `risk_tabs` 這個 widget 只在「風險分析」開啟時才存在，
   對不存在的 widget 寫 session_state 會被丟掉——結果是「渲染沒有例外」但分頁根本沒開，
   診斷時「比較基準」selectbox 是空的才看出來。**「沒有例外」不等於「有渲染」。**
   既有測試也只驗證三個子分頁存在、從未選取它們。
   改為直接呼叫三個 renderer（與子分頁執行的是同一段程式）。

**39 項涵蓋**：匯率序列品質、混合幣別取數與標示、主頁計算、跨市場行事曆下的連續區段、
夏普、Beta、配息（VT 除息換算後最大單次 35.14 元＝美元 1.11 × 匯率）、
AI payload、七個預設配置的比較、分割偵測仍有效、以及五個 renderer 的實機渲染。

### 2026-09-14（第四十五次）—— 退休6 加進去了但畫面看不到：兩層原因
`session: claude-ff [3b52fc]`

使用者回報「本機重啟還是看不到退休6」。程式碼是對的（七個預設配置都在、
退休6 實測 1 年 +22.2%、回撤 −7.0%、波動 14.0%，VT 正確標示為已換算），
問題在**兩件事疊在一起**，兩者都已寫進環境陷阱 5b：

1. `launch.py` 刻意設計成可重複執行，偵測到同一個 App 還在跑就**重用不重開**。
   證據：伺服器行程 06:36:15 啟動，而 `allocation.py` 06:41:20 才改。
2. `_refresh_replaced_modules()` 比對「.pyc 對磁碟原始碼」而非「記憶體對磁碟」，
   而編輯後跑過的 pytest 會在**另一個行程**裡 import 同一批模組、
   把 `.pyc` 重寫成新的時間戳——伺服器於是判定「沒有東西被換過」。
   實測三個模組的 .pyc 內外時間戳全部相符。

**雲端不受影響**：那裡沒有第二個 Python 行程會重寫 .pyc，
所以第三十一次的設計對它原本的用途仍然成立。這是本機開發限定的盲點。

**沒有修那個機制**。要修是讓它同時比對「記憶體裡上次看到的 mtime」（約 4 行），
但那是第三十、三十一次花兩輪才穩定下來的安全機制，
而 WORKLOG 明確警告過「無差別重載會撤銷行程在那些模組上做過的任何設定」
（第三十一次打壞 `test_app_controls_and_empty_state` 的那次）。要改就單獨做一輪測試。

**判斷「有沒有真的重啟」的方法**：`app/launch.py --check` 印 `START` 代表可啟動、
`REUSE` 代表已在跑；或比對行程啟動時間與程式檔 mtime。

### 2026-09-14（第四十六次）—— `run.bat` 改成強制重啟：殺得夠準，殺不掉就不要裝作成功
`session: claude-d8 [b4b834]`

使用者要求 `run.bat` 強制重啟。這是在拆環境陷阱 5b 的第一半——
「雙擊 `run.bat` 不等於重啟」害第四十五次整則都在查一個不存在的程式錯誤。

**做法**：`launch.py` 加 `--restart`，`run.bat` 與 `start.ps1` 都帶上。

**只殺自己人。** 殺的對象不是「8501–8510 上的任何東西」，而是**先問過 app id 端點、
確認回的是 `taiwan-market-lens-0908` 的那些埠**。既有的 `is_our_app()` 本來就是為了
「這個埠上的是不是我」而寫的，正好拿來當殺人前的身分驗證。
否則使用者在 8502 跑的別的 Streamlit 專案會被順手殺掉，而且不會有任何跡象。

**殺不掉就整個放棄，不要改用別的埠。** 這點是刻意的：
若 taskkill 失敗（例如行程屬於沙箱帳號 `MSI\CodexSandboxOffline`，見陷阱 1）而我們
還是往下走，`select_port()` 會找到 8502 並**成功開起來** ——
使用者得到一個「看起來重啟成功」的新視窗，裡面跑的卻是新程式、而舊視窗還在 8501 上活著。
那比不重啟更糟。現在直接 return 1 並印出 taskkill 的原話。

**netstat 的 State 欄不能用。** 要從埠找到 PID 只能解析 `netstat -ano`，
但 State 那欄在部分語系的 Windows 會被翻譯，比對 `"LISTENING"` 就會全部漏掉。
改成認 Foreign Address 是萬用位址（`0.0.0.0:0` / `[::]:0`）——這欄不會被翻譯。
（本機 zh-TW 實測 State 仍是英文的 `LISTENING`，但這不保證別台也是，不值得賭。）
`test_listeners_are_found_even_when_windows_translates_the_state_column` 用德文欄位鎖住。

**實測數據**：taskkill 之後 port **0.0 秒**就可以 bind（沒有 TIME_WAIT 擋 listener），
所以等待迴圈實際上都是第一次就過；仍保留最多 10 秒的等待，因為「殺完馬上搶不到原本那個埠」
會讓強制重啟默默換埠，正是上面要避免的事。
端到端實測：8501 上 PID 22308 → 執行 `--restart` → 同一個埠變成 PID 21616。

**代價（使用者要知道）**：每次雙擊都是完整重啟，Streamlit 冷啟動 ~5 秒，
而且 `@st.cache_data` 的行情快取在行程裡，跟著一起清空——下一次渲染會重打 Yahoo。
只想「開瀏覽器看已經在跑的那個」就直接跑 `app/launch.py`（不加旗標），舊行為原封不動還在。

**舊視窗會印一行錯誤，沒有去消掉它。** 被殺的是 `launch.py` 的子行程，
父行程 `process.wait()` 拿到 1 並回傳，於是舊的 `run.bat` 走進 `errorlevel 1` 分支。
要讓它乾淨退出就得分辨「我是被新的啟動器換掉」與「streamlit 自己炸了」，
而唯一可靠的辦法是死掉後再等幾秒看有沒有人接手那個埠——那會讓**真正的**啟動失敗
慢好幾秒才回報，不划算。改成把 `run.bat` 那句話寫準：講「這個視窗結束了」而不是「啟動失敗」。

**順手記一個測試時撞到的既有缺陷（沒修）**：`--check` 在 App 開機的那幾秒會誤報 `START`。
那時埠已綁住但 app id 端點還沒起來，`is_our_app` 與 `port_free` 同時為假，
`select_port()` 就跳到下一個埠。已寫進陷阱 5b。

**`start.ps1` 一起改了**，雖然使用者只說 `run.bat`：README 把兩者列為等價入口，
留一個重用一個重啟，就是替下一個接手的人造新的 5b。

131 → 137 passed（新增 6 個）。

### 2026-09-14（第四十七次）—— 美股名稱改向 Yahoo 要，但只在台股名錄查不到時
`session: claude-d8 [b4b834]`

使用者問「VOO QQQ TQQQ 等等的名稱有機會取得嗎」。畫面上 VT 有中文名而 VOO 顯示
「名稱暫未取得」，差別純粹是 **VT 被手寫進 `market.py` 的 `CATALOG`**，不是哪裡壞掉——
名稱的兩個來源（`CATALOG` 與證交所 ISIN 名錄）都不含美股。

**新來源是 `yf.Search`**，不是 `Ticker.info`。兩者都拿得到名稱、速度也相近（0.6s），
但 `.info` 是出了名的脆弱與肥大；`Search` 回的是報價列表，要的欄位就在裡面。
**把 `news_count` / `lists_count` / `recommended` 都設 0 之後從 0.6s 降到 0.28s**——
預設會連新聞和推薦清單一起抓回來，那些我們一個都不用。

**`shortname` 不能直接用：Yahoo 把它截在 31 字，而且是攔腰截。**
實測 `State Street SPDR S&P 500 ETF T`、`Vanguard Total World Stock Inde`、
`YUANTA SECURITIES INV TRUST CO `。所以規則是「shortname 短於 31 字就用它，否則用 longname」
（longname 比較囉唆但完整：`Vanguard Total World Stock Index Fund ETF Shares`）。

**搜尋是模糊比對，必須要求代碼完全相符。** 查 `VO` 會回 VOO、VOOG、VOOV，
拿第一筆就會把別人的名字掛到使用者的標的上。查無此代碼時乾淨回空清單（實測 `ZZZZQQ`）。

**查不到也要快取。** `label()` 在相關性分頁是每個配對呼叫一次（`insights.py:174` 是 O(n²)），
失敗不快取的話，Yahoo 一次斷線就會變成「每次 render 都重打十幾次、每次都等到逾時」。
所以成功與失敗一律進 `@st.cache_data(ttl=3600)`，代價是斷線後最多一小時內都顯示
「名稱暫未取得」——那是純外觀損失，比整頁變慢划算。

**台股一定不查。** `currency_of(symbol) == "USD"` 才查。這條不是最佳化而是正確性：
實測 `0050.TW` 在 Yahoo 查到的是 `YUANTA SECURITIES INV TRUST CO `，
一旦讓它有機會覆蓋，中文名錄就會被英文名蓋掉。
`test_a_taiwan_listing_never_asks_yahoo_for_its_name` 用 `assert_not_called()` 鎖住。

**優先序是 CATALOG > ISIN 名錄 > Yahoo。** 手寫的名字永遠贏，所以 VT 仍然是
「Vanguard全世界股票」而不是英文。要讓 VOO 也顯示中文，就在 `CATALOG` 加一行——
**但沒有代為加**：美股 ETF 沒有官方中文名，自己編一個等於憑空造資料。

**兩條 label 路徑都改了。** 主分頁用 `streamlit_app.py` 裡的區域 `label()`（吃 ISIN 名錄），
其餘五個分頁用 `market.label()`（只吃 CATALOG）。只改前者的話，VOO 會在相關性、夏普、
Beta、投資報酬、配置比較顯示成「VOO VOO」。

**實測**：VOO / QQQ / TQQQ / SPY / SCHD / ARKK / BRK-B 全部取得，各約 0.28s、
快取後 0.000s。`^TWII` 因為在 `TAIWAN_INDICES` 裡所以不查。

**順手記一個沒修的既有缺陷**：`market.label()` 對「不在 CATALOG 裡的台股」會把市場後綴
當成名稱印出來——`00865B.TW` 在那五個分頁顯示為「00865B TW」。
它出現在退休5／退休6 兩個預設配置裡，所以看得到。要修得讓 `market.py` 也拿得到 ISIN 名錄
（目前只有 `streamlit_app.py` 有），那是另一輪的事。

137 → 142 passed（新增 5 個，含一個 AppTest 驗選單上真的看得到名稱）。

### 2026-09-14（Codex量化補充）—— 同樣150萬生活費、不足都質押，排名取決於股票超額報酬
`session: 01a09d9b-8e2f-7fc2-972a-f773acf622df`

使用者明確更正：退休6只代理退休5；退休5年領=(成長池4%+緩衝池)25%，借款=150萬-提領。故第四十、四十一次「零借款／零破產」不適用本次規則。

新增獨立 analysis/retirement_compare.py、帳務測試與 RETIREMENT_COMPARISON.md，沒有改APP。3000萬、40年、固定名目150萬、利息3%資本化、盈餘先還債；退休1沿用固定94.05萬年配息另測可變配息。4000條配對12月區塊重抽，以負債/資產>=50%作研究警戒線，絕非真實斷頭線。跨線後續算僅為診斷，不能視作銀行同意借款。

主要發現：原始歷史退休1/5觸50%占比4.9%/21.4%；把台股、QQQ、全球股票源序列年複合報酬同調8%後變49.5%/25.1%。股票同8%加通膨2%變66.4%/44.2%。全球維持8%，台股/QQQ需約10–11%才翻排名。本樣本原始台股13.68%、QQQ16.31%、全球8.48%，不能把過去超額报酬當未來必然。原始歷史退休1高期末值也受正二不再平衡後漂移影響。

新資料陷阱：本次Yahoo TWD=X下載在2004-11至2006-04整段缺失，舊紀錄2004起22.5年不可直接照用。本次只用2006-06至2026-08的243連續月，沒有补造缺月。VT前用SPY/EFA/EEM合成；BIL前SHY；LQD天期較短故另測VCLT替換。全為代理，不是009826的真實40年績效。

5個新增帳務測試全過，沒有重跑或聲稱APP全套測試。完整假設、數字與來源見analysis報告。結果是條件式排名，不能宣稱任何一方已保證足付150萬。

### 2026-09-14（Codex量化補充二）—— 兩邊生活費等於退休5提領，退休5不借款
`session: 01a09d9b-8e2f-7fc2-972a-f773acf622df`

使用者的新條件取代前次固定150萬元：每條路徑每年先算C=(退休5成長池4%+緩衝池)25%，兩者都花C；退休5零借款，退休1借max(C-00984B配息,0)，配息盈餘先還債、無債則回債券。**沒有150萬上限**，C上升超過150萬時兩邊也同步提高支出。若原意是min(150,C)，不可套用本次結果，需另跑。

新增獨立analysis/retirement_linked_spending.py與LINKED_SPENDING_COMPARISON.md，保留前次固定150萬腳本／results.json。首年共同支出102萬，退休1借7.95萬，退休5借0；未計利息時淨資產相同2898萬。退休1配息仍假設每年94.05萬，另測按市值6.27%和固定67.5萬；不是基金保證配息。

同前次243個月、4000條40年配對12月重抽。原始歷史退休1觸50%負債比0.7%；股票同8%變48.6%，利率5%變57.6%，改長公司債代理45.0%；退休5均0%，**是規則導出的無債結果，不能稱作退休成功率100%**。股票同8%且以通膨2%折算：第10/20/40年生活費中位購買力116.8/132.8/159.9萬；第40年第5百分位33.9萬；37.3%路徑曾低於75萬。生活費风险兩邊相同，退休5免去借款風險但仍可能大幅減少支出。

資產側仍不能只看無債：股票同8%時50.8%路徑為退休1不觸50%且期末淨資產較高；48.6%曾觸線。跨線後續算僅診斷，不可當銀行願持續借。月末檢查、代理、不含2000泡沫與費稅等限制沿用前報告。

10個研究帳務測試全過（前5＋新增5），抓到並修正單年樣本無年度變化率時應回None。APP未修改，未跑或宣稱APP全套通過。所有新輸出為linked_*，完整公式／結果／重跑命令見analysis/LINKED_SPENDING_COMPARISON.md。後續agent請先辨認固定150萬與本次浮動C是兩個不同問題。

### 2026-09-14（策略圖解）—— 原始100：12版，與既有90：10模擬分開
`session: 01a09d9b-8e2f-7fc2-972a-f773acf622df`

使用者要求產生策略說明圖，已存analysis/retirement5-original-100-12.png。圖示原始100：12、成長池每年移4%至緩衝池後領25%，3000萬首年107.14萬。含忽略短債報酬時的生活費平滑公式，以及成長池100跌至60、生活費4降至3.6的靜態例子。這不是新回測；前面研究仍用90：10、首年102萬，沒有改APP預設。原始100：12的長期重跑尚未進行。圖由imagegen生成，數字與公式已目視核對；無固定收入或四年支出保證。

### 2026-09-14（第四十八次）—— 變更紀錄改成由舊到新，新的一則加在最後面
`session: claude-d8 [b4b834]`

使用者規定：**每一則標題都要有日期，而且最新的要在最後。** 規則已寫進檔案開頭與
「變更紀錄」標題下方——寫在標題下方是因為要補一筆的人會直接捲到那裡，不會回頭讀前言。

**51 則全部倒轉**（原本是新的在最上面）。日期不用補：檢查過 51 則標題本來就都帶日期，
待驗證與「已評估但決定不做」那兩節的 `###` 也都有。

**倒轉不是單純 reverse。** Codex 那三則是接在舊檔（新在前）的**最下面**，
但它們在時間上最晚（10:17–11:17，比第四十六、四十七次的 09:15–09:23 晚）。
天真地整批反轉會把最新的三則送到最前面。正確做法是只倒轉前 48 則，再把那三則按原順序接在尾端。

**倒轉反而修好了三處相對指涉。** 第二十八次與第十六次的標題寫著「更正上一則／前一則的錯誤判斷」、
第二十四次寫著「是前一則改動引進的」，指的都是**時間上更早**的那則。
在舊排法下那則在它們**下方**，字面是錯的；倒轉後剛好在正上方。
（用編號的交叉引用如「見第四十三次」不受影響，這也是編號比方位可靠的理由。）

**順手統一行尾。** Codex 那三則是單獨 LF，夾在整份 CRLF 的檔案裡，
會讓 diff 與部分編輯器的分段判斷出錯。已全部正規化為 CRLF。

改動方式是腳本＋斷言：倒轉後比對「51 則的內容集合」與倒轉前完全相同，確認沒有任何一則被改到或掉字。

### 2026-09-14（第四十九次）—— `analysis/` 進版控，連原始下載一起
`session: claude-d8 [b4b834]`

Codex 的退休比較研究（前三則）原本整個目錄都沒進版控。已提交，31 個檔案、約 8.9 MB。

**`analysis/data/` 的 11 份原始行情 CSV 是重點，不是附屬品。**
`retirement_compare.py` 的 `download()` 寫著 `if p.exists(): continue` ——
有檔就不重抓。所以這些 CSV 就是研究的實際輸入；少了它們，下一個人重跑會靜默
拿到**不一樣的資料**而不自知。TWD=X 就是活生生的例子：Codex 這次抓到的缺了
2004-10 至 2006-05，跟第四十三次記載的不同（見該則與下面的待辦）。
提交這些檔案是把「可重跑」變成「可重現」。

**`.gitignore` 加了一條例外 `!analysis/*.log`。** 兩份報告都把 `run.log`／`linked_run.log`
列為佐證，但原本的 `*.log` 會把它們擋掉——進了版控也帶不走，引用會變成死連結。
順帶一提 `linked_run.log` 其實不是 log：它是 17 KB 的單行 JSON，副檔名取錯了。
`__pycache__/` 仍然照擋。

**這些測試不在 APP 測試套件裡**，這是刻意的：`pytest.ini` 的 `testpaths = app/tests`。
研究測試用 unittest，要另外跑：

```powershell
.venv\Scripts\python.exe -m unittest discover -s analysis -p "test_retirement*.py"
```

已實跑：研究 10 passed、APP 142 passed，兩邊都綠。

**體積最大的是 1.8 MB 的策略說明 PNG**，其次是七份各約 700 KB 的行情 CSV。

**更正（第五十次）**：本則原本寫「若日後覺得 repo 太肥，先砍的應該是 PNG」。
**那個建議現在是錯的**——第五十次把那張圖放上 APP 的「緩衝池退休法」分頁，
它已經是執行期資產。`data/` 也砍不得（砍了就失去可重現性）。兩者都要留。

### 2026-09-14（第五十次）—— 新增「緩衝池退休法」分頁：解釋規則，不是推薦規則
`session: claude-d8 [b4b834]`

使用者要求用一個分頁說明退休5／退休6 的成長池＋緩衝池策略，詳述每個數字的用意，並貼上
`analysis/retirement5-original-100-12.png`。新增 `app/retirement_strategy.py`，
排在「AI 深度解讀」之後、「關於與使用說明」之前。

**內容全部是既有紀錄的搬運，沒有新的模擬。** 引用的數字出自第四十、四十一次
（最差單年收入 −12.9% vs 無緩衝池 −37.1%、存續期間 7.00→0.21 年、均衡點代數、
2008 那段緩衝池佔比自動從 10% 跳到 17.9% 再回來、通膨 5% 下實質收入幾乎不變）。
**代價也一起寫進去**：要 150 萬得有 4,200 萬本金、中位路徑第 11 年才首次超過 150 萬、
最大回撤 −49.5% 比退休1 的 −34.3% 深。只講好處會變成推薦，不是說明。

**四張卡片與步驟表是現算的，不是寫死的。** `pool_plan()` 是純函式，
`growth_share()` 直接讀 `PRESETS['退休5']` —— 有人改了預設權重，這一頁會跟著改。
寫死 90/10 的話，這頁會在沒人發現的情況下開始說謊。

**圖和 APP 預設的比例不一樣，這點必須講明。** 圖畫的是原始 100：12（首年 107.14 萬），
APP 預設是比較好記的 90：10（首年 102 萬）。分頁用一個 radio 讓使用者切換，
**預設停在 APP 自己的配置**——這頁的職責是解釋 APP 在做什麼，圖的說明文字則標注它是另一個比例。
順帶測出一件有意思的事：100：12 下緩衝池正好是 3.00 年提領量、第一年進出完全打平；
90：10 是 2.94 年，支出略小於撥入，緩衝池會非常緩慢地變厚。
`test_the_rounder_ratio_leaves_the_buffer_a_little_short_of_three_years` 鎖住這個差異。

**那張 PNG 現在是執行期資產。** 分頁直接讀 `analysis/` 底下的檔案（沒有複製一份進 `app/`，
1.8 MB 複製兩份沒有意義）。檔案不在時退化成一則提示、不中斷，但那種退化很難被發現，
所以加了一個測試斷言檔案存在。**第四十九次「太肥先砍 PNG」的建議已就地更正。**

**踩到的坑**：`ui.wan()` 在 100 萬以上不留小數（`300 萬` 而不是 `300.0 萬`），
我照直覺寫的測試期望值是錯的。另外既有的 `test_risk_tabs_group_and_default_to_correlation`
把七個分頁標籤寫死比對，加分頁一定會踩到它——這是刻意的設計，不是脆弱的測試。

**驗證**：AppTest 選取分頁後確認 subheader 與三張卡片的數字真的渲染出來
（第四十四次的教訓：沒有例外不等於有渲染），另外在本機實跑 `launch.py --restart`，
確認新模組在真的伺服器上 import 得起來——AppTest 抓不到陷阱 5 那類問題。

142 → 150 passed（新增 8 個）。

### 2026-09-14（第五十一次）—— 緩衝池分頁補上執行面：25% 是年度比率，不是月度比率
`session: claude-d8 [b4b834]`

使用者指出實務做法：撥款一年執行一次，但**領出的 25% 不必一次領完，可以分月動用**。
原本的分頁只寫規則不寫執行，讀起來像是每年得一次把整年生活費提出來。已補一節。

**真正該防的是另一件事：分月動用不等於每月重算。** 25% 是年度比率。
若有人把它當月度比率、每個月重新算一次緩衝池的 25%，一年會領走 **96.8%**（1 − 0.75¹²），
緩衝立刻消失——那是完全不同的規則，不是不同的時程。這點用 `st.warning` 獨立標出，
因為它是這個規則最容易被誤用的地方，而誤用的後果不可逆。

**分月動用實際值多少錢，算給使用者看。** 年初一次領出等於假設那筆錢整年沒在賺錢；
分十二次領，未動用的部分平均還留在池裡 **11/24 年**。
以 00865B 實測年化 2.89% 估，102 萬的生活費大約多 **1.35 萬／年**（約生活費的 1.3%）。
金額不大，但方向是對使用者有利的，而且它同時說明**模擬結果偏保守**——
`retirement_linked_spending.py` 是 `if m%12==0` 把整年支出一次扣掉、之後才套當年報酬。

**11/24 是推導出來的，不是湊的。** 每月初領十二分之一，第 k 個月那份少賺 (1−k/12) 年，
整年合計 6.5/12；相對於一次領出的 1.0，差額正好 11/24。
`test_spreading_the_withdrawal_is_worth_the_months_it_stays_invested` 直接用級數重算一次來驗，
而不是把 0.4583 寫進斷言——後者只會鎖住我可能算錯的結果。

**踩到的坑（工具層，不是程式）**：用 heredoc 傳 Python 腳本去改檔案時，
字串裡的 `\n` 會在抵達 Python 之前就被解成真正的換行，於是比對永遠找不到目標。
要比對原始碼裡字面上的反斜線，得用 `chr(92)` 組出來。

150 → 152 passed（新增 2 個）。

### 2026-09-14（第五十二次）—— 緩衝池分頁砍掉三個小節，代價段移到「關於與使用說明」
`session: claude-d8 [b4b834]`

使用者指定移除三節：「代價，要記在同一本帳上」「退休5 與退休6 差在哪」「這頁的數字是哪裡來的」。
分頁剩六節：一年只做兩個動作、換成你的金額、執行面、每個數字為什麼是那個數字、
為什麼不需要再平衡、這個設計買到了什麼。

**移除後分頁只剩好處那張表，沒有對價。** 這件事有跟使用者講過，是他的決定。
但把免責與資料來源整段刪掉是另一回事——那些字本來就不屬於「說明策略」，而屬於
「這個 APP 告訴你的東西有多可信」。**所以搬到 `about.md` 的對應段落，沒有憑空消失**：
模擬用的是歷史代理序列不是產品實績、009826 只有幾週所以不存在長期回測、
規則的代價（本金要求較高、收入波動、回撤較深、給不出保證金額）、未計稅費、非投資建議。
若使用者要的是連 `about.md` 也不要提，再說一聲即可移除。

**順手清掉 `allocation_card` 的 import**——它只被「退休5 與退休6 差在哪」用到。
留著不會報錯，但會讓下一個人以為這頁還在畫配置卡片。

測試不受影響：那幾節沒有斷言，AppTest 驗的是 subheader 與三張卡片的數字。

**但這次整套跑了兩趟，第一趟是紅的**，記下來免得下次以為是新問題：
`test_a_running_deployment_heals_a_function_that_lost_an_argument` 失敗一次，
單獨重跑過、整套重跑也過（152 passed）。**間歇性，與本次改動無關**（那幾節沒有測試）。
已排除最明顯的嫌疑：`_deployed_over()` 已經把 mtime 推 +10 秒，不是 .pyc 一秒解析度的問題。
**還沒找到成因，不要假裝找到了。** 一個未經驗證的猜想是：那兩趟都在本機 App 跑著的時候執行，
而伺服器行程會重寫 `.pyc`（陷阱 5b 的反面），時機湊巧就會打壞這個依賴 .pyc 戳記的測試。
要驗就是關掉 App 連跑數趟、再開著 App 連跑數趟比對，本次沒做。
**在查清楚之前，看到這個測試紅了先重跑一次再說。**

### 2026-09-15（第五十三次）—— 一天沒有收盤價的資料列，把每一檔都從畫面上拿掉了
`session: claude-09 [a371a6]`

使用者回報「APP 取不到股價」。**不是網路問題，是 `unit_breaks()` 撞到 NaN 直接丟例外**，
`load_frame` 把它收成 failure，於是那一檔整個被排除。實測四個預設配置：
衝刺 1/3 檔失敗、退休1 4/4 全失敗、退休5 2/2 全失敗、退休6 2/2 全失敗。

**觸發條件是 Yahoo 在 2026-09-14 這一列給了成交量卻沒給收盤價。**
0050 成交量 91,668,562 而 Close／Adj Close 是 null；00662、VT 同樣如此，
2330 則正常。台股與美股都中，所以不是單一市場的問題。

**NaN 有兩次機會被擋下來，兩次都漏掉，因為 NaN 的所有比較都回 False**：
`previous <= 0 or current <= 0` 為假，接著讓平常日子通過的那個帶寬
`1-SPLIT_STEP < factor < ...` 也為假（所以不 continue），最後死在 `round(nan)`。
修法是在取完 `float()` 之後加一道 `isfinite` 檢查就好——用 `isfinite` 而不是
既有的 `x == x` 慣用法，是因為它同時擋掉 inf（`round(inf)` 會丟 OverflowError）。

**這是潛伏已久的缺陷，不是哪個 commit 改壞的。** Adj Close 任何位置出現 NaN 都會炸，
只是 Yahoo 以前很少吐 null 收盤價。**雲端跑的是同一份程式，所以正式站當時應該也是壞的。**

**畫面訊息把人引去錯的方向。** 全部失敗時 `streamlit_app.py` 顯示
「目前無法取得行情。請檢查網路或稍後重新取得資料。」——網路是好的。
**這則訊息沒有改**，因為它對真正的網路失敗仍然正確；要分辨得看 failure 的原文，
而那個已經在「資料問題與處理方式」展開區裡了。

**排除掉的假線索**：直接打 `query2.finance.yahoo.com` 的 chart 端點會拿到
**429 Too Many Requests**，但 yfinance 走 cookie／crumb 那條路是通的（243 筆全拿到）。
429 與本次無關，不要浪費時間在限流上。

**新測試先確認過會紅。** 把護欄拿掉，兩個新測試都失敗在 `market.py:232` 的 ValueError，
裝回去才綠——否則鎖不住任何東西。第二個測試鎖的是「跳過那一組不能連帶跳過別的」：
序列中間有一天沒有價格時，後面的分割仍然要被校正。

**NaN 那天本來就會被丟掉**，`compare_prices()` 的 `dropna(how="any")` 負責，
所以共同期間自然收在 2026-09-11，不需要為此再改任何東西。

152 → 154 passed。另外實跑 `smoke_live.py` **39 項全過**（真實行情、混合幣別、
逐一渲染每個分頁），以及 `launch.py --restart` 確認本機 8501 上換成新程式。

### 2026-09-15（第五十四次）—— 首頁等的不是行情，是 8 MB 的證券名錄
`session: claude-09 [a371a6]`

使用者要「優化存取速度」。**先量再改**，結果瓶頸完全不在意料中的地方。

**量到的（同一台機器、同一段時間）：**

| 項目 | 冷啟動 |
|---|---|
| `load_names()` 證交所 ISIN 名錄 | **11–15 秒** |
| `load_frame()` 三檔股價 | **0.27 秒** |
| `fx_rates()` TWD=X period=max | 0.095 秒 |
| 單檔 `load_symbol()` | 0.15 秒 |
| `_refresh_replaced_modules()` 每次 rerun | 5 毫秒 |
| import pandas / streamlit / altair / yfinance | 0.6 / 0.8 / 0.25 / 0.25 秒 |

**股價根本不慢。** 慢的是那份名錄：上市那頁是 **8.2 MB 的 HTML**，下載 4 秒、
`HTMLParser` 解析 **5.7–7.2 秒**；上櫃 2.7 MB 再加約 3.6 秒。兩邊並行，牆鐘 11–15 秒。
而 `streamlit_app.py` 的 `with comparison_tab:` 第一行就是 `load_names()`，
**所以每次冷啟動，使用者都在對著空白頁等一份中文名錄。**

**兩個改動：**

**1. 先給快照，網路版在背後追上。** `data/securities.json` 本來就隨 app 出貨，
而且實測 **2,334 筆 vs 線上 2,335 筆** —— 只差一檔當週新上市的 `7825.TWO`。
所以先回快照（3.6 毫秒），背景執行緒去抓，下一次 rerun 就換成新的。
**代價是一檔新上市會晚幾秒出現**，對照的是整頁晚十幾秒出現。

**2. 解析改用正規表示式。** `HTMLParser` 逐字走完 8 MB；regex 版在上市快 6.4 倍、
上櫃快 11.2 倍。**不是憑感覺換掉的** —— 拿兩份線上名錄實跑，輸出 `==` 完全相同才敢改。
順手補了 `html.unescape()`：舊的 HTMLParser 沒有覆寫 `handle_entityref`，
名稱裡若出現 `&amp;` 會被**整個丟掉**；現在會還原成 `&`。
**目前的名錄裡沒有這種名字**，所以這是預防性的，不是修 bug。

**效果（stash 前後在同一台機器各量兩次，避免拿冷磁碟的數字灌水）：**

- 首次渲染 **15.2 / 22.6 秒 → 2.0 / 3.7 / 3.8 / 6.2 秒**
- `load_names()` 本身 **約 11,000 毫秒 → 3.6 毫秒**（背景跑完後 0.11 毫秒）
- 之後的 rerun **沒有變慢**：背景執行緒跑完前後量都是 ~1.0 秒，差異是機器雜訊

**一個要自我更正的數字**：最早量到基準線 32.9 秒，那裡面含了冷磁碟與 .pyc 冷啟動。
同機器重量是 15–23 秒，**這才是誠實的對照基準**。

**踩到的兩件事：**

- **`fx_rates()` 第一次量到 11.6 秒，是雜訊。** 乾淨行程重量是 0.685 秒、
  暖機後 0.095 秒。差點為此去改 `period="max"`，**還好先重量了一次**。
- **背景執行緒的例外不能讓它自己飛掉。** 第一版只有 try/finally，
  pytest 就報 `PytestUnhandledThreadExceptionWarning` —— 那代表失敗只會留在
  server log 裡，而側邊欄還在若無其事地用快照的名字。改成攔下來、把兩個市場都記為失敗，
  使用者才看得到「名稱更新暫不可用」。

**其他設計決定：**

- **還沒失敗不算失敗。** 第一次呼叫回傳的 `failed` 是空的，不是「上市、上櫃」——
  抓取只是還沒跑完，這時候警告使用者是謊報。
- **抓失敗時 10 分鐘就重試**（`RETRY_SECONDS`），不是等滿 6 小時。
  期間畫面用的是快照，所以重試不花使用者任何東西。
- **`_fetched_at` 不論成敗都會蓋章**，否則某個來源掛掉時會變成每次 rerun 都開一條新執行緒。
- **名錄是整個行程共用的**，不是每個連線抓一次。`test_a_second_visitor_does_not_start_a_second_fetch` 鎖住這件事。

**API 變了一個地方**：`load_names` 不再是 `@st.cache_data`，所以 **沒有 `.clear()` 了**，
測試改用 `forget_names()` / `await_refresh()`。`test_app.py` 整個 patch 掉 `load_names`，不受影響。

**查過但不值得動的**：`_refresh_replaced_modules()` 每次 rerun 只花 5 毫秒；
altair 只佔 0.25 秒，為它做延遲 import 不划算；pandas 與 streamlit 是 Streamlit 自己
先 import 的，動不了。**這一個改動已經吃掉幾乎全部可得的空間。**

154 → 160 passed（新增 6 個）。另外 `smoke_live.py` 39 項全過、研究測試 10 項全過。
