# WORKLOG —— 工作日誌

> 這個專案由多個 AI agent 輪流接手（ChatGPT Codex、Claude Code），各自在不同的
> 作業階段與帳號下執行。**接手前先讀這份，再讀 `README.md` 與 `app/about.md`。**
>
> **每次改動都要在「變更紀錄」補一筆。** 只寫從程式碼和 `git log` 看不出來的東西：
> 為什麼這樣選、試過什麼結果不行、量到的數據、還沒決定的事、踩過的環境雷。
> 程式碼本身說得清楚的（改了哪個函式、加了哪個參數）不要抄進來。
>
> 這份檔案**進版控**，因為它的用途就是跟著 repo 交接給下一個接手的人或 agent。
> 真正的金鑰放 `.streamlit/secrets.toml`（已 gitignore），任何情況都不要寫進這裡。

---

## 目前狀態

**最後更新：2026-09-11**

- 分支：`feature/ai-allocation-advisor`，基底 `99b5a42`（origin/main）
- 已 commit `4f8029d`（10 檔、+823/-33），**工作區乾淨、尚未 push**
- 要更新雲端正式站需合回 `main` 再 push（App settings 沒有 branch 欄位）
- 測試：**63 passed / 0 failed**（全綠）
- 本機 App：`http://127.0.0.1:8501`，AI 分頁串流輸出，首字約 1–10 秒、全文約 9–20 秒

## 進行中

把 AI 解讀從「附屬在相關性分頁的小面板」做成完整功能。原始需求是
**「分析並評論 APP 上設定的投資配置，並提出專業的理財規劃建議」**。

分四步，目前完成第 1 步：

1. ✅ AI 解讀獨立成分頁，放在「投資報酬」之後
2. ✅ 放寬 system prompt 分寸，改為輸出「樣貌／風險／方向」三段評論
3. ⬜ 補上理財規劃需要的輸入（投資年期、每月可投入、目標、風險承受度）
4. ⬜ 視情況做 BYO key（讓使用者用自己的 HF token）

## 待決定

**第 3 步還沒做**：payload 缺理財規劃需要的脈絡（投資年期、每月可投入、
目標金額、風險承受度），app 目前沒有這些輸入。沒有它們，AI 只能講配置通則，
給不出真正個人化的規劃討論。這是「理財規劃建議」與「配置評論」的分界線。

---

## 環境陷阱

踩過的，會再踩的：

**1. `.git` 的擁有者不是 `ssp62`。**
Codex 在沙箱帳號 `MSI\CodexSandboxOffline` 下跑，`.git` 歸它所有。
以 `ssp62` 執行 git 會被 dubious ownership 擋下。已加例外：

```bash
git config --global --add safe.directory 'C:/Data/project/Codex/Codex APP 0908'
```

程式檔案本身（`app/**`）owner 是 `ssp62`，ACL 有 `Authenticated Users: Modify`，
兩個帳號都寫得進去。**但不要兩個 agent 同時改同一個檔案。**

**2. 主控台是 cp950，中文會變亂碼、emoji 會直接炸。**
所有會輸出中文的指令前面加 `PYTHONIOENCODING=utf-8`。

**3. `.pytest_cache` 寫入被拒（屬於沙箱帳號）。**
跑測試加 `-p no:cacheprovider`，否則會噴一堆 PytestCacheWarning：

```bash
PYTHONIOENCODING=utf-8 ./.venv/Scripts/python.exe -m pytest -q -p no:cacheprovider
```

**4. Streamlit 不會重新 import 子模組。**
hot-reload 只重跑主腳本，`import` 進來的模組留在 `sys.modules` 快取。
**新增函式、新增模組、改 import 結構之後一定要重啟服務**，否則會出現
「Streamlit 顯示的原始碼是新的、實際執行的是舊的」這種極度誤導的 traceback。
（`app/module_compat.py` 的 `load_renderer` 就是為了對付這件事，但它救不了自己。）

**5. 雲端部署不會帶上 `secrets.toml`。**
`.streamlit/secrets.toml` 在 `.gitignore` 裡（正確）。
部署後要去 Streamlit Cloud 的 App settings → Secrets 手動貼一次。
建議雲端另外產一把 token，方便單獨撤銷。
另外 App settings **沒有 branch 欄位**，分支在建立 app 時決定、事後改不了，
所以更新正式站就是合回 `main` 再 push。

---

## Hugging Face 實測數據

用 `.streamlit/secrets.toml` 的 token 實際打出來的，**不是查文件推測的**。
要改模型或調參數之前先看這裡，可以省掉重測的錢和時間。

**計費**：免費帳號每月 $0.10 credits，用完要主動買才能繼續（不會自動扣款）。
PRO $2.00／月。實測單次呼叫 `estimated_cost` 約 $0.00004–0.00015。

**同一個模型在不同供應商的行為不一樣。**`:fastest` / `:cheapest` 後綴不只影響
速度和價格，還會換掉思考模式的預設值。以下是 1100 max_tokens、相同 payload 的實測：

| 模型與後綴 | enable_thinking | 耗時 | 正文 | 推理 | finish_reason |
|---|---|---|---|---|---|
| `GLM-4.7-Flash:fastest` | **False** | **3.1s** | 275 字 | 0 | stop ✅ |
| `GLM-4.7-Flash:fastest` | 預設 | 11.2s | **0 字** | 1667 字 | length ❌ |
| `GLM-4.7-Flash:cheapest` | False | 78.2s | 2664 字 | 0 | length ⚠️ |
| `GLM-4.7-Flash:cheapest` | 預設 | 29.7s | 394 字 | 0 | stop |
| `Qwen3.5-35B-A3B:fastest` | **False** | **3.1s** | 374 字 | 0 | stop ✅ |
| `Qwen3.5-35B-A3B:fastest` | 預設 | 8.5s | **0 字** | 3295 字 | length ❌ |

**結論**：`:fastest` + 明確傳 `chat_template_kwargs: {"enable_thinking": False}`。
不要依賴供應商預設，也不要用 `:cheapest`（慢，且輸出長度不穩定）。

繁中品質兩個模型都可用。GLM 在測試題上抓到「單一國家**與**單一產業」的雙重集中度，
Qwen 只講了國家那一層。DeepSeek 系列要留意簡體用詞（「投资组合」「盈利」）。

---

## 變更紀錄

### 2026-09-11（第九次）—— 移除 AI 分頁上重複的指標與結論

使用者指出組合三張指標卡與三行文字結論在這一頁是多餘的 —— 它們在
「相關性與分散效果」分頁已經有了，擺在這裡只是把真正的 AI 內容往下推。

兩者現在只作為 payload 材料存在（`conclusions()` 仍照常呼叫、`portfolio_stats()`
的結果仍送進 `allocation_facts()`），**只是不再渲染**。
分頁保留配置與期間兩行 caption 當作「現在看的是什麼」的脈絡，其餘直接進 AI 面板。


### 2026-09-11（第八次）—— 指示可編輯、標示模型、加上 HF 標記

**指示改成可編輯**（`st.text_area` + 套用／恢復預設，存在 `session_state['system_prompt']`）。

**必須一起處理的地雷：快取鍵要含 prompt。** 原本 key 是 `sha256(model + payload)`，
prompt 改了但 payload 沒變就會直接回舊快取，看起來像「改了沒用」。
現在是 `sha256(model + prompt + payload)`。`SYSTEM_PROMPT` 也從模組常數改成
`request_insight` / `stream_insight` 的參數。

**安全取捨**：預設指示裡有防止模型自行計算、誇大幅度、給出買賣指令的規則。
使用者移除後這些限制就沒了，所以自訂時畫面會出現警告，
產出文字下方也標示「使用自訂指示」，避免跟預設指示產出的內容混為一談。
修改只存在當次連線，不寫入任何檔案，不影響其他使用者。

**修掉一個設計缺陷**：透明度區塊原本在 token 檢查**之後**，
所以沒設定 AI 時完全看不到「啟用後會送出什麼」。現在移到檢查之前，
未設定模型也看得到指示與數字（也因此 AppTest 不需要 secrets 就能測）。

**模型說明**：新增 `MODEL_NOTES`，以模型代號（去掉 `:fastest` 後綴）對應簡短說明。
**內容只寫本專案實測到的結果**，不抄廠商宣稱 —— 例如 Qwen 那則直接寫明
「實測會出現簡體字、夾雜英文並捏造事實，因此未採用」，讓後人知道為什麼不選它。

**HF 標記**用 🤗 emoji，不嵌入官方 logo 圖檔：這就是 Hugging Face 自己的品牌符號，
不必為此增加二進位資產與授權考量。

**測試**：新增 4 個（編輯套用與還原的往返、自訂 prompt 有送進請求、
未指定時用預設、`model_note` 要忽略路由後綴）。59 → 63 passed。


### 2026-09-11（第七次）—— 連 prompt 一起公開

原本展開區只顯示數字，改成顯示**送給 AI 的完整內容**：指示（system prompt）與數字兩部分。
理由是使用者有權知道 AI 被要求用什麼角度解讀、哪些話不准講 ——
只公開數字而不公開指示，透明度只做了一半。

用 `st.code(SYSTEM_PROMPT, language=None, wrap_lines=True, height=320)` 原文顯示，
不用 `st.markdown`：prompt 裡有 `##` 標題，渲染後會跟 AI 的實際輸出混淆，
而且使用者要看的是「模型收到的原文」，不是排版後的樣子。附帶複製按鈕方便外部比對。

新增 `test_ai_tab_shows_the_prompt_not_just_the_numbers` 鎖住這個性質，
避免日後重構讓 prompt 悄悄從畫面消失。
做過變異驗證：把 `st.code(...)` 換成 `pass`，測試如預期失敗。58 → 59 passed。


### 2026-09-11（第六次）—— Beta 與配息串進 AI（一律送出，不設選項）

使用者要求不做選用勾選。折衷做法：**資料在按下按鈕時才抓，不是開啟分頁時**——
不用勾選，開分頁依然秒開，只有真的要 AI 分析才付網路成本。
`ai_panel(payload, expand)` 的 `expand` 是個 callable，在按鈕分支裡才執行；
`payload` 基礎版仍即時產生，供快取鍵與透明度展開區使用。

**成本我原本高估了。** 實測冷快取（用從未抓過的標的、全新行程）只要 **0.4 秒**，
不是我擔心的數秒級。yfinance 的配息端點很快，`load_distributions` 又有 1 小時快取。

新增 `beta_facts()`、`dividend_facts()`，以及 `market_and_income_facts()` 把兩者
**各自獨立 try**：一邊掛掉不能連累另一邊，缺哪邊就在 payload 放 `資料缺漏` 欄位，
並要求模型明講該面向無法取得而不是略過（prompt 規則 11）。

**踩到一個假象**：基準預設是 `0050.TW`，而它常常同時是持股。
對自己回歸必然得到 Beta 1.00、R2 1.00，模型會誤讀成「完美追蹤大盤」。
payload 現在會在這種情況下加一個 `注意` 欄位明講這兩個數字不具意義。

**指標有鑑別力**（兩個組合實測對照）：

| | 衝刺（0050+2330+2454） | 高股息（0056+00878+00919） |
|---|---|---|
| 零配息月份 | 8 / 14 | 1 / 14 |
| 單月最高佔全期 | 45.5% | 12.3% |
| 組合 Beta | 1.16（R2 0.80） | 0.52（R2 0.62） |

**新增兩條防呆規則**（同樣都是實測抓到的真實錯誤）：

- **Beta / 配息的典型誤讀**：低 Beta 不等於低風險；R2 低時不可用 Beta 下判斷；
  除息不是獲利、配息率不可與報酬率相加；除息日不是付款日。
- **不得使用數字不支持的程度用語**。模型曾把 -20.6% 的回撤寫成「價格腰斬」——
  腰斬是 -50%。現在禁止「腰斬」「崩盤」「重挫」這類自行加碼的嚴重程度。

加規則後輸出品質明顯提升，出現了這種真正有用的判斷：
「組合夏普比率 2.69，略低於 0050 元大台灣50 的 2.72，顯示每承受一單位波動
所換取的超額報酬並未優於直接持有指數。」—— 挑三檔電子股還不如直接買 0050。

**測試**：新增 3 個失敗隔離測試（一邊掛不能連累另一邊、兩邊都掛要明講、
都正常時不可出現缺漏欄位）。55 → 58 passed。


### 2026-09-11（第五次）—— 修好 test_app.py 的 6 個紅燈

根因是 `99b5a42` 的 `preset_picker()` 在首次載入就套用「衝刺」配置，
把選股從測試 patch 的 `DEFAULT_SYMBOLS`（10 檔）換成預設配置的 3 檔。

**還有第二個被遮住的破口**：預設配置的下拉選單插在側邊欄最前面，
`selectbox[0]` 從「價格基準」變成「預設配置」，所以
`app.selectbox[0].select("收盤價（未還原）")` 也早就壞了 ——
只是測試在更前面的斷言就失敗，看不到這一個。

**三個修法**：

1. `new_app()` 預設設 `portfolio_presets_initialized=True` 跳過預設配置，
   讓每個測試自己決定標的。這些測試本來就不是在測預設配置，
   是預設配置干擾了它們的前置設定。
2. 新增 `pick(app, kind, label)` 用**標籤**查找 widget 取代索引。
   索引查找只要上方插入任何 widget 就會壞 —— 這次就是這樣壞的。
3. 新增 `test_preset_applies_on_first_load`，真正測預設配置的行為
   （首次載入套用衝刺 3 檔 + 1000 萬、切換到退休變 4 檔 + 3000 萬）。
   **預設配置原本完全沒有測試，這正是它當初改壞別的測試卻沒人發現的原因。**

新測試做過變異驗證：把衝刺的 `2454.TW` 改成 `2603.TW`，測試如預期失敗。

48 → 55 passed，全綠。


### 2026-09-11（第四次）—— 串上夏普、改為串流輸出

**夏普已餵給 AI，Beta 與配息還沒。** 三者成本差很多：

| 項目 | 現況 | 成本 |
|---|---|---|
| 夏普 | `sharpe_stats()` 是純函式、零網路 | 幾乎免費，**已完成** |
| Beta | `fit_beta()` 純函式，但要抓 `^TWII` 基準 | 多一個網路請求與失敗路徑 |
| 配息 | 每檔都要 `load_distributions()` | 最重，N 個網路請求 |

**設計考量**：AI 分頁目前不觸發任何額外網路請求，只用已載入的行情。
配息原本被刻意放在 `st.toggle('載入配息試算')` 後面就是為了避免自動抓取。
硬串進來會讓開啟 AI 分頁變慢很多，違背這個專案的延遲載入設計。
若要做，建議設計成**選用勾選**（「一併分析 Beta 與配息（需額外抓取資料）」），
讓分頁預設保持快速。

新增 `sharpe_facts()`。無風險利率從 `st.session_state['sharpe_rate']` 讀，
但因為延遲渲染，使用者可能從沒開過夏普分頁 —— 預設 `DEFAULT_RISK_FREE_RATE = 2.0`
並在 payload 裡標明假設值。整段包在 try 裡：夏普是加分項，不該擋住其餘解讀。

prompt 新增防呆規則：**夏普為負代表低於無風險利率，不等於虧損**（最常見的誤讀），
且組合夏普不是各檔夏普的加權平均。實測輸出遵守了。

---

**改成串流輸出（`st.write_stream`）。** 這是為了解決一個已經咬到的問題：
加了 payload 之後出現一次 **>90 秒逾時**，整個功能失敗。

| 方式 | 首字 | 總時間 |
|---|---|---|
| 非串流 | 全程空白 | 8.4–11.7s，偶發 >90s ❌ |
| 串流 | **1–10s** | 8.9–20.4s |

串流同時解決兩件事：使用者馬上看到文字開始跑，而且
**讀取逾時變成「chunk 之間」而非總生成時間** —— 慢供應商不再造成整體失敗。
延遲變異是供應商端的，縮短 prompt 犧牲品質並不能解決，串流才是正解。

實作上 `request_insight`（緩衝版）保留給腳本檢查，UI 走 `stream_insight`。
共用的請求組裝與空回應判斷抽成 `_body()` / `_guard()`，避免兩條路徑各自長歪。
`ai_panel` 串流完成後把結果寫進 session 快取，**並刻意不再 `st.markdown` 一次**
（文字已經在畫面上，重畫會變兩份）。

**測試**：新增 4 個串流測試，含「畸形 chunk 不可中斷整段解讀」——
SSE 解析是新的失敗面，不能沒有測試守著。43 → 48 passed。


### 2026-09-11（第三次）—— 專業分析框架 prompt + 補齊 payload

**判斷**：改 prompt 只是一半。原本 payload 只有三句結論、相關配對、比重、兩個期間 ——
分析師拿到三個數字也只能講通則。**瓶頸在資料不在措辭。**

新增純函式 `allocation_facts()`，把專業配置檢視需要的數字全部在 Python 算好：

- 每檔的比重／區間報酬／年化波動／最大回撤（依比重由大到小排序）
- 集中度：最大單一比重、前二大合計、HHI、**有效持股檔數**（1/HHI）、實際檔數
- 組合：區間報酬、年化波動、加權個別波動、**波動差距（百分點）**、
  最大回撤、**回撤回復所需漲幅**

「回撤回復所需漲幅」特別重要：跌 20.5% 要漲 25.8% 才回得來。
這個不對稱性是專業檢視的核心概念，**但它是除法** —— 一定要在 Python 算好再給，
不能讓模型自己算。

**順手修掉一個既有缺陷**：payload 裡 `weights` 用代碼（`0050.TW`）、`pairs` 用中文名，
模型很難把兩邊對起來。現在統一用顯示名稱。
（實作時踩到自己的坑：`rename(index=label)` 會把 `等權重組合` 也套上 `label()`
變成「等權重組合 名稱暫未取得」，害 `volatility[portfolio_name]` KeyError。
改用字典映射，只換持股不動組合列。）

**SYSTEM_PROMPT 重寫成四段分析框架**：配置結構／分散效果檢驗／風險特徵／權衡與方向。

**逐句檢查輸出後，補了三條防呆規則**（每一條都是實測抓到的真實錯誤，不是預防性的）：

1. **回撤不對稱不可寫成「僅需」。** 模型曾寫「僅需上漲 25.8% 即可回到原點」——
   把不對稱性講反了，重點是「必須漲更多」。
2. **代價必須與方向邏輯一致。** 模型曾寫「引入低相關資產（如債券）但這會**增加**
   對半導體的曝險」—— 完全相反，是為了湊「代價」硬掰的。
3. **「有效持股檔數」只衡量比重，不衡量相關性。** 模型曾寫「有效檔數等於實際檔數，
   代表分散完全未發揮作用」—— 講反了，兩者相等代表比重完全平均，是比重層面最分散的狀態。
   分散有沒有效要看相關性與波動差距。

**教訓：加了規則之後三個錯誤都消失，但這是逐句人工核對才抓到的。
改 prompt 之後一定要實際跑一次並逐句檢查，不能只看「讀起來很專業」。**

**測試**：新增 5 個涵蓋 HHI、有效檔數、回復漲幅必大於跌幅、波動差距、排序。38 → 43 passed。

**延遲**：9–36 秒，變異很大（同樣輸入量過 8.9s / 20.7s / 36.4s）。
90 秒逾時仍有緩衝，但如果之後覺得等太久，正解是改用串流輸出（`st.write_stream`），
不是縮短 prompt。

**還沒做**：仍缺投資年期／每月可投入／目標金額／風險承受度（app 根本沒有輸入欄位），
沒有這些就只能給「配置評論」，給不出「理財規劃建議」。


### 2026-09-11（第二次）—— prompt 改為產出評論，並標示產生模型

**問題**：第一版上線後實測，模型只是把 payload 條列複述，不帶觀點。
而且複述時出錯：把「組合年化波動」和「加權個別波動平均」當成同一個數字，
又把「較 N/M 檔標的淺」寫成讀起來像比例。**模型沒有理解數字的角色，只是搬運。**

**改法**：system prompt 拉成 `SYSTEM_PROMPT` 常數並重寫，要求輸出三段二級標題
（這個配置的樣貌／值得注意的風險／可以考慮的方向），明文要求「不要逐條複述數字」，
並針對上述兩個誤讀寫了防呆規則。允許模型運用對標的所屬產業的一般認識討論集中度，
但數值一律只能引用輸入。

**法規分界（刻意保留的禁止項）**：具體買賣指令與時點、個股推薦、報酬或走勢預測、
保證性用語。台灣的投資顧問是特許業務，配置層面的觀察可以，買賣指令不行。
頁尾免責也改成「教育性資訊，不構成投資建議，也不是個人化理財規劃」。

**顯示模型**：session 快取從存字串改為存 `{'text', 'model'}`，
輸出下方標示實際產生的模型 ID。使用者要求的，也方便比較不同模型的效果。

**模型選擇覆核（實測，同一 prompt 與 payload）**：

| 模型 | 耗時 | tok/s | 品質 |
|---|---|---|---|
| `GLM-4.7-Flash:fastest` | 12–15s | 25–37 | ✅ 乾淨繁中、無捏造 |
| `Qwen3.5-35B-A3B:fastest` | 3.3–3.8s | 132 | ❌ 見下 |

Qwen 快 4 倍但**不能用**：出現簡體字「负相關」（prompt 明文禁止仍漏）、
混入英文「該 sector」、捏造「透過調整權重」（根本沒有這回事）、
語意不通的句子、並把 prompt 的免責原文照抄進輸出。
**財經文字出現簡體字與捏造事實，比慢 10 秒嚴重得多 —— 維持 GLM。**

另註：GLM 曾出現一次 70 秒的離群值（供應商打嗝），連測三次穩定在 12–15 秒。
90 秒逾時的設定有足夠緩衝，不要調低。



### 2026-09-11 —— AI 解讀獨立成分頁 + 修掉三個真實缺陷

**新增「AI 深度解讀」分頁**（在「投資報酬」之後、「關於與使用說明」之前）。

關鍵設計：**新分頁必須自給自足。** 這個 app 用 `tab.open` 做延遲渲染，
而 AI 面板原本寄生在「相關性與分散效果」分頁算出來的 `volatility` / `drawdown` 上 ——
使用者如果從沒點開那一頁，資料根本不存在。
所以把組合統計抽成純函式 `correlation.portfolio_stats()`，AI 分頁自己算自己要的。

「數據解讀」的文字結論**刻意留在相關性分頁**，因為它是緊貼上方圖表的說明，
搬走會讓那頁少一塊。AI 分頁有自己的配置摘要、期間、指標卡，
外加一個可展開的「送出的統計摘要」，讓使用者看得到 AI 到底收到哪些數字。

**修掉三個缺陷**（都是實測才浮出來的，看程式碼看不出來）：

1. **推理模型會讓功能永久顯示「AI 暫時無法回應」。**
   原本只讀 `content`，空字串就 `raise ValueError('Empty response')`，
   被 `ai_panel` 的 `except` 接住後顯示籠統錯誤。
   使用者會以為是網路或額度問題，其實是模型選錯，而且完全沒有線索。
   現在偵測到 `content` 空但 `reasoning_content` 有內容時，
   拋 `ModelOutputError` 並說明原因。

2. **讀取逾時 30 秒太短。** 這是使用者實際撞到的錯誤。
   `:cheapest` 路由實測要 40.2 秒。已改 90 秒（`READ_TIMEOUT_SECONDS`）。

3. **`max_tokens` 700 會把回答切在半句。**
   `:fastest` 實測 `out=700` 剛好撞上限。已改 1100，
   並加上截斷偵測：`finish_reason == 'length'` 時在文末標註「內容可能未完整」。
   財經文字講一半卻看起來像完整結論，比直接失敗更危險。

**順手**：`module_compat.load_renderer` 原本只在「函式少了新參數」時重載，
但雲端可能整個函式都還沒有，那會直接 AttributeError。改成 `function is None` 也重載。

**測試**：新增 9 個（組合統計加權正確性、payload 期間欄位、相關配對去重與 NaN、
推理模型錯誤路徑、截斷標記、模組重載）。29 → 38 passed。
原本寫死 `timeout == (5,30)` 的斷言改成引用常數，以後調參數不會再假性失敗。

**走過的彎路**：一開始推薦 `:cheapest` 是只看單價、沒考慮延遲，
在互動式 app 裡是錯的取捨。另外一度以為「GLM-4.7-Flash 預設不 thinking」是模型屬性，
實測後發現那只在特定供應商成立。**涉及供應商行為的結論一定要實測，不要靠一次觀察外推。**
